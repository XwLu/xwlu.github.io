<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>Multi-Thread Abstract on 朝花夕拾</title>
        <link>https://example.org/post/coding/c&#43;&#43;/multi-thread/</link>
        <description>Recent content in Multi-Thread Abstract on 朝花夕拾</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>en-us</language>
        <lastBuildDate>Wed, 02 Apr 2025 10:32:39 +0800</lastBuildDate><atom:link href="https://example.org/post/coding/c++/multi-thread/index.xml" rel="self" type="application/rss+xml" /><item>
            <title>Async &amp; Future</title>
            <link>https://example.org/post/coding/c&#43;&#43;/multi-thread/async-future/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/multi-thread/async-future/</guid>
            <description>&lt;h1 id=&#34;前言&#34;&gt;前言&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;在阅读Apollo Planning代码的dp_st_graph部分的时候，遇到future这个多线程接口，特地来记录一下。&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;hr&gt;&#xA;&lt;h1 id=&#34;用法一等待task执行完毕&#34;&gt;用法一：等待task执行完毕&#xA;&lt;/h1&gt;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;#include &amp;lt;iostream&amp;gt;&#xA;#include &amp;lt;future&amp;gt;&#xA;#include &amp;lt;vector&amp;gt;&#xA;&#xA;using namespace std;&#xA;void task(string c){&#xA;  for (int i = 0; i &amp;lt; 10; i++){&#xA;    cout&amp;lt;&amp;lt;c;&#xA;  }&#xA;}&#xA;&#xA;int main(){&#xA;  vector&amp;lt;future&amp;lt;void&amp;gt;&amp;gt; results;&#xA;  results.emplace_back(std::async(std::launch::async, task, &amp;#34;A&amp;#34;));&#xA;  results.emplace_back(std::async(std::launch::async, task, &amp;#34;B&amp;#34;));&#xA;  results.emplace_back(std::async(std::launch::async, task, &amp;#34;C&amp;#34;));&#xA;  &#xA;  for (int i = 0; i &amp;lt; 10; i++){&#xA;    cout&amp;lt;&amp;lt;&amp;#34;D&amp;#34;;&#xA;  }&#xA;&#xA;  for (auto&amp;amp; result : results){&#xA;    result.get();//强制阻塞main线程，直到对应的task线程执行完毕&#xA;  }&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;hr&gt;&#xA;&lt;h1 id=&#34;用法二同步获取异步结果&#34;&gt;用法二：同步获取异步结果&#xA;&lt;/h1&gt;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;#include &amp;lt;iostream&amp;gt;&#xA;#include &amp;lt;future&amp;gt;&#xA;#include &amp;lt;vector&amp;gt;&#xA;&#xA;&#xA;using namespace std;&#xA;&#xA;int task(string c){&#xA;  if (c == &amp;#34;A&amp;#34;){&#xA;    std::this_thread::sleep_for(chrono::seconds(1));&#xA;    cout&amp;lt;&amp;lt;c&amp;lt;&amp;lt;endl;&#xA;    std::this_thread::sleep_for(chrono::seconds(1));&#xA;    cout&amp;lt;&amp;lt;c&amp;lt;&amp;lt;endl;&#xA;    std::this_thread::sleep_for(chrono::seconds(3));&#xA;    cout&amp;lt;&amp;lt;c&amp;lt;&amp;lt;endl;&#xA;    return 1;&#xA;  }&#xA;&#xA;  else if (c == &amp;#34;B&amp;#34;){&#xA;    std::this_thread::sleep_for(chrono::seconds(1));&#xA;    cout&amp;lt;&amp;lt;c&amp;lt;&amp;lt;endl;&#xA;    std::this_thread::sleep_for(chrono::seconds(1));&#xA;    cout&amp;lt;&amp;lt;c&amp;lt;&amp;lt;endl;&#xA;    std::this_thread::sleep_for(chrono::seconds(2));&#xA;    cout&amp;lt;&amp;lt;c&amp;lt;&amp;lt;endl;&#xA;    return 2;&#xA;  }&#xA;&#xA;  else{&#xA;    std::this_thread::sleep_for(chrono::seconds(1));&#xA;    cout&amp;lt;&amp;lt;c&amp;lt;&amp;lt;endl;&#xA;    std::this_thread::sleep_for(chrono::seconds(1));&#xA;    cout&amp;lt;&amp;lt;c&amp;lt;&amp;lt;endl;&#xA;    std::this_thread::sleep_for(chrono::seconds(1));&#xA;    cout&amp;lt;&amp;lt;c&amp;lt;&amp;lt;endl;&#xA;    return 3;&#xA;  }&#xA;&#xA;}&#xA;&#xA;int main(){&#xA;  vector&amp;lt;future&amp;lt;int&amp;gt;&amp;gt; results;&#xA;  results.emplace_back(std::async(std::launch::async, task, &amp;#34;A&amp;#34;));&#xA;  results.emplace_back(std::async(std::launch::async, task, &amp;#34;B&amp;#34;));&#xA;  results.emplace_back(std::async(std::launch::async, task, &amp;#34;C&amp;#34;));&#xA;&#xA;  cout&amp;lt;&amp;lt;&amp;#34;D&amp;#34;&amp;lt;&amp;lt;endl;&#xA;&#xA;  for (auto&amp;amp; result : results){&#xA;    int tmp = result.get();//异步结束的task，同步获取返回值&#xA;    cout&amp;lt;&amp;lt;&amp;#34;get &amp;#34;&amp;lt;&amp;lt;tmp&amp;lt;&amp;lt;endl;&#xA;  }&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;&#xA;&lt;li&gt;三个task结束的时间不一样，但是程序获取task返回值是一起获取的，&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
        </item><item>
            <title>Mutex</title>
            <link>https://example.org/post/coding/c&#43;&#43;/multi-thread/mutex/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/multi-thread/mutex/</guid>
            <description>&lt;h1 id=&#34;锁基本背景&#34;&gt;锁基本背景&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;C++提供了四种锁：互斥锁、条件变量、自旋锁和读写锁&lt;/li&gt;&#xA;&lt;li&gt;C++11引入了&lt;strong&gt;std::unique_lock&lt;/strong&gt;与&lt;strong&gt;std::lock_guard&lt;/strong&gt;&lt;/li&gt;&#xA;&lt;li&gt;boost库实现了&lt;strong&gt;读写锁&lt;/strong&gt;&lt;/li&gt;&#xA;&lt;li&gt;当一个线程中的函数获取了锁，在锁没有消失(超出作用域)前，另一个线程获取该锁的操作(函数)将无法进行。&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;hr&gt;&#xA;&lt;h1 id=&#34;读写锁&#34;&gt;读写锁&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;主要适合在于共享数据更新频率较低，但是读取共享数据频率较高的场合。&lt;/li&gt;&#xA;&lt;li&gt;当 data 被线程A读取时，其他线程仍可以进行读取却不能写入&#xA;&lt;ul&gt;&#xA;&lt;li&gt;当线程A获得共享锁时，其他线程仍可以获得共享锁但不能获得独占锁&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;当 data 被线程A写入时，其他线程既不能读取也不能写入&#xA;&lt;ul&gt;&#xA;&lt;li&gt;当线程A获得独占锁时，其他线程既不能获得共享锁也不能获得独占锁&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;#include &amp;lt;boost/thread/shared_mutex.hpp&amp;gt;&#xA;#include &amp;lt;boost/thread/shared_lock_guard.hpp...&#xA;#include &amp;lt;boost/thread.hpp&amp;gt;&#xA;void demo()&#xA;{&#xA;    typedef boost::shared_lock&amp;lt;boost::shared_mutex&amp;gt; read_lock;&#xA;    typedef boost::unique_lock&amp;lt;boost::shared_mutex&amp;gt; write_lock;&#xA;    boost::shared_mutex read_write_mutex;&#xA;    int32_t data = 1;&#xA;    //线程A,读data&#xA;    {&#xA;        read_lock rlock(read_write_mutex);&#xA;        std::cout &amp;lt;&amp;lt; data &amp;lt;&amp;lt; std:; endl;&#xA;    }&#xA;    //线程B,读data&#xA;    {&#xA;      read_lock rlock(read_write_mutex);&#xA;      std::cout &amp;lt;&amp;lt; data &amp;lt;&amp;lt; std:; endl;&#xA;    }&#xA;    //线程C,写data&#xA;    {&#xA;      write_lock rlock(read_write_mutex);&#xA;      data = 2;&#xA;    }&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;h3 id=&#34;注意上面的锁的作用域就是锁所在的&#34;&gt;注意：上面的锁的作用域就是锁所在的&#xA;&lt;/h3&gt;&lt;hr&gt;&#xA;&lt;h1 id=&#34;互斥锁&#34;&gt;互斥锁&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;&#xA;&lt;h3 id=&#34;简单用法&#34;&gt;简单用法&#xA;&lt;/h3&gt;&lt;ul&gt;&#xA;&lt;li&gt;std::lock_guard 和 std::unique_lock一样&lt;/li&gt;&#xA;&lt;li&gt;当锁在生命周期之内，可以实现加锁，通常利用花括号控制加锁的范围&lt;/li&gt;&#xA;&lt;li&gt;简单使用&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;#include &amp;lt;mutex&amp;gt;          // std::mutex, std::lock_guard&#xA;std::mutex mut;&#xA;void insert_data(){&#xA;  std::lock_guard&amp;lt;std::mutex&amp;gt; lk(mut);&#xA;  queue.push_back(data);&#xA;}&#xA;void process_data(){&#xA;  std::unqiue_lock&amp;lt;std::mutex&amp;gt; lk(mut);&#xA;  queue.pop();&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;&#xA;&lt;h3 id=&#34;进阶用法&#34;&gt;进阶用法&#xA;&lt;/h3&gt;&lt;ul&gt;&#xA;&lt;li&gt;std::unique_lock更加灵活，但开销也更大&lt;/li&gt;&#xA;&lt;li&gt;具体用法见下一节&lt;strong&gt;条件变量&lt;/strong&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;hr&gt;&#xA;&lt;h1 id=&#34;条件变量&#34;&gt;条件变量&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;相关的类包括&#xA;&lt;ul&gt;&#xA;&lt;li&gt;std::condition_variable&lt;/li&gt;&#xA;&lt;li&gt;std::condition_variable_any&lt;/li&gt;&#xA;&lt;li&gt;std::cv_status枚举类型&lt;/li&gt;&#xA;&lt;li&gt;std::notify_all_at_thread_exit()&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;std::condition_variable&#xA;&lt;ul&gt;&#xA;&lt;li&gt;当&lt;strong&gt;std::condition_variable&lt;/strong&gt;对象的某个wait 函数被调用的时候，它使用&lt;strong&gt;std::unique_lock&lt;/strong&gt;(通过 std::mutex)来锁住当前线程。当前线程会一直被阻塞，直到另外一个线程在相同的 &lt;strong&gt;std::condition_variable&lt;/strong&gt; 对象上调用了 &lt;strong&gt;notification&lt;/strong&gt; 函数来唤醒当前线程。&lt;/li&gt;&#xA;&lt;li&gt;&lt;strong&gt;std::condition_variable&lt;/strong&gt;对象通常使用&lt;strong&gt;std::unique_lock&lt;/strong&gt; 来等待，如果需要使用另外的&lt;strong&gt;lockable&lt;/strong&gt;类型，可以使用&lt;strong&gt;std::condition_variable_any&lt;/strong&gt;类&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;#include &amp;lt;thread&amp;gt;&#xA;#include &amp;lt;mutex&amp;gt;&#xA;#include &amp;lt;condition_variable&amp;gt;&#xA;void demo()&#xA;{&#xA;  std::mutex mtx; // 全局互斥锁.&#xA;  std::condition_variable cv; // 全局条件变量.&#xA;  bool ready = false; // 全局标志位.&#xA;&#xA;  //线程A&#xA;  {&#xA;    std::unique_lock &amp;lt;std::mutex&amp;gt; lck(mtx);&#xA;    while(!ready)&#xA;    cv.wait(lck);// 当前线程被阻塞, 当全局标志位变为 true 之后,线程被唤醒, 继续往下执行打印线程编号id.&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;thread &amp;#34; &amp;lt;&amp;lt; id &amp;lt;&amp;lt; &amp;#39;\n&amp;#39;;&#xA;  }&#xA;&#xA;  //线程B&#xA;  {&#xA;    std::unique_lock &amp;lt;std::mutex&amp;gt; lck(mtx);&#xA;    ready = true;&#xA;    cv.notify_all(); // 唤醒所有线程.&#xA;  }&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
        </item><item>
            <title>Thread</title>
            <link>https://example.org/post/coding/c&#43;&#43;/multi-thread/thread/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/multi-thread/thread/</guid>
            <description>&lt;h1 id=&#34;创建线程&#34;&gt;创建线程&#xA;&lt;/h1&gt;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;#include &amp;lt;iostream&amp;gt;&#xA;#include &amp;lt;thread&amp;gt;&#xA;using namespace std;&#xA;&#xA;void t1()  //普通的函数，用来执行线程&#xA;{&#xA;  for (int i = 0; i &amp;lt; 20; ++i)&#xA;  {&#xA;    cout &amp;lt;&amp;lt; &amp;#34;t1111\n&amp;#34;;&#xA;  }&#xA;}&#xA;void t2()&#xA;{&#xA;  for (int i = 0; i &amp;lt; 20; ++i)&#xA;  {&#xA;    cout &amp;lt;&amp;lt; &amp;#34;t22222\n&amp;#34;;&#xA;  }&#xA;}&#xA;int main()&#xA;{&#xA;  thread th1(t1);  //实例化一个线程对象th1，使用函数t1构造，然后该线程就开始执行了（t1()）&#xA;  thread th2(t2);&#xA;&#xA;  cout &amp;lt;&amp;lt; &amp;#34;here is main\n\n&amp;#34;;&#xA;&#xA;  return 0;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;h4 id=&#34;tip-上面的代码存在一个问题主线程结束之后其他两个线程也会直接结束引起异常报错&#34;&gt;Tip: 上面的代码存在一个问题，主线程结束之后，其他两个线程也会直接结束，引起异常报错&#xA;&lt;/h4&gt;&lt;hr&gt;&#xA;&lt;h1 id=&#34;join方法&#34;&gt;join方法&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;join的作用是让主线程在join函数处等待，直到该子线程执行结束，再往后执行&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;#include &amp;lt;iostream&amp;gt;&#xA;#include &amp;lt;thread&amp;gt;&#xA;using namespace std;&#xA;&#xA;void t1() {&#xA;  for (int i = 0; i &amp;lt; 20; ++i) {&#xA;    cout &amp;lt;&amp;lt; &amp;#34;t1111\n&amp;#34;;&#xA;  }&#xA;}&#xA;void t2() {&#xA;  for (int i = 0; i &amp;lt; 20; ++i) {&#xA;    cout &amp;lt;&amp;lt; &amp;#34;t22222\n&amp;#34;;&#xA;  }&#xA;}&#xA;int main() {&#xA;  thread th1(t1);&#xA;  thread th2(t2);&#xA;&#xA;  th1.join(); //等待th1执行完&#xA;  th2.join(); //等待th2执行完&#xA;&#xA;  cout &amp;lt;&amp;lt; &amp;#34;here is main\n\n&amp;#34;;&#xA;&#xA;  return 0;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;这样，当两个线程执行完之后，主程序才会执行cout并退出&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;hr&gt;&#xA;&lt;h1 id=&#34;detach方法&#34;&gt;detach方法&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;detach是用来和线程对象分离的，这样线程可以独立地执行，不过这样由于没有thread对象指向该线程而失去了对它的控制，当对象析构时线程会继续在后台执行，但是当主程序退出时并不能保证线程能执行完&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;int main() {&#xA;  thread th1(t1);&#xA;  thread th2(t2);&#xA;&#xA;  th1.detach();&#xA;  th2.detach();&#xA;&#xA;  cout &amp;lt;&amp;lt; &amp;#34;here is main\n\n&amp;#34;;&#xA;&#xA;  return 0;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;h4 id=&#34;tips-如果没有良好的控制机制或者这种后台线程比较重要最好不用detach而应该使用join&#34;&gt;Tips: 如果没有良好的控制机制或者这种后台线程比较重要，最好不用detach而应该使用join&#xA;&lt;/h4&gt;&lt;hr&gt;&#xA;&lt;h1 id=&#34;注意&#34;&gt;注意&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;调用 join 或 detach 之前需要调用 joinable() 判断一下线程是否运行. 如果 joinable() 返回 false, 则不需要.&lt;/li&gt;&#xA;&lt;li&gt;需要注意的是线程对象执行了join后就不再joinable了，所以只能调用join一次&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;hr&gt;&#xA;&lt;h1 id=&#34;调用类内对象&#34;&gt;调用类内对象&#xA;&lt;/h1&gt;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;class Demo {&#xA;Public:&#xA;  Demo(;&#xA;  ~Demo();&#xA;&#xA;  void Run(int a, int b);&#xA;}&#xA;&#xA;int main(char agrc, char** argv) {&#xA;  Demo* pDemo;&#xA;  std::Thread* pT;&#xA;  pDemo = new Demo();&#xA;  pT = std::thread(&amp;amp;Demo::Run, pDemo, 1, 2);&#xA;  //第一个参数是函数,第二个参数是类(如果是在类内创建就用this),  后面就是函数传入的参数&#xA;  if(pT-&amp;gt;joinable()) {&#xA;    pT-&amp;gt;join();//Program will wait here until the end of Run().&#xA;  }&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;hr&gt;&#xA;&lt;h1 id=&#34;cmakeliststxt&#34;&gt;CMakeLists.txt&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;需要链接pthread库，否则编译无法通过:&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;TARGET_LINK_LIBRARIES(your_executable pthread)&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
        </item><item>
            <title>ThreadPool</title>
            <link>https://example.org/post/coding/c&#43;&#43;/multi-thread/thread-pool/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/multi-thread/thread-pool/</guid>
            <description>&lt;h1 id=&#34;代码&#34;&gt;代码&#xA;&lt;/h1&gt;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;#include &amp;lt;vector&amp;gt;&#xA;#include &amp;lt;queue&amp;gt;&#xA;#include &amp;lt;memory&amp;gt;&#xA;#include &amp;lt;thread&amp;gt;&#xA;#include &amp;lt;mutex&amp;gt;&#xA;#include &amp;lt;condition_variable&amp;gt;&#xA;#include &amp;lt;future&amp;gt;&#xA;#include &amp;lt;functional&amp;gt;&#xA;#include &amp;lt;stdexcept&amp;gt;&#xA;#include &amp;lt;iostream&amp;gt;&#xA;#include &amp;lt;chrono&amp;gt;&#xA;&#xA;class ThreadPool {&#xA;public:&#xA;  ThreadPool(size_t);&#xA;  template&amp;lt;class F, class... Args&amp;gt;&#xA;  auto enqueue(F&amp;amp;&amp;amp; f, Args&amp;amp;&amp;amp;... args) &#xA;      -&amp;gt; std::future&amp;lt;typename std::result_of&amp;lt;F(Args...)&amp;gt;::type&amp;gt;;&#xA;  ~ThreadPool();&#xA;private:&#xA;  // need to keep track of threads so we can join them&#xA;  std::vector&amp;lt; std::thread &amp;gt; workers;&#xA;  // the task queue&#xA;  std::queue&amp;lt; std::function&amp;lt;void()&amp;gt; &amp;gt; tasks;&#xA;  &#xA;  // synchronization&#xA;  std::mutex queue_mutex;&#xA;  std::condition_variable condition;&#xA;  bool stop;&#xA;};&#xA;&#xA;inline ThreadPool::ThreadPool(size_t threads) : stop(false)&#xA;{&#xA;  for(size_t i = 0; i &amp;lt; threads; ++i) {&#xA;    workers.emplace_back(&#xA;      [this]&#xA;      {&#xA;        for(;;)&#xA;        {&#xA;          std::function&amp;lt;void()&amp;gt; task;&#xA;          {&#xA;            std::unique_lock&amp;lt;std::mutex&amp;gt; lock(this-&amp;gt;queue_mutex);&#xA;            this-&amp;gt;condition.wait(lock,&#xA;              [this]{ return this-&amp;gt;stop || !this-&amp;gt;tasks.empty(); });&#xA;            if(this-&amp;gt;stop &amp;amp;&amp;amp; this-&amp;gt;tasks.empty())&#xA;              return;&#xA;            task = std::move(this-&amp;gt;tasks.front());&#xA;            this-&amp;gt;tasks.pop();&#xA;          }&#xA;          task();&#xA;        }&#xA;      }&#xA;    );&#xA;  }&#xA;}&#xA;&#xA;// the destructor joins all threads&#xA;inline ThreadPool::~ThreadPool()&#xA;{&#xA;  {&#xA;    std::unique_lock&amp;lt;std::mutex&amp;gt; lock(queue_mutex);&#xA;    stop = true;&#xA;  }&#xA;  condition.notify_all();&#xA;  for(std::thread &amp;amp;worker: workers)&#xA;    worker.join();&#xA;}&#xA;&#xA;// add new work item to the pool&#xA;template&amp;lt;class F, class... Args&amp;gt;&#xA;auto ThreadPool::enqueue(F&amp;amp;&amp;amp; f, Args&amp;amp;&amp;amp;... args) &#xA;  -&amp;gt; std::future&amp;lt;typename std::result_of&amp;lt;F(Args...)&amp;gt;::type&amp;gt;&#xA;{&#xA;  using return_type = typename std::result_of&amp;lt;F(Args...)&amp;gt;::type;&#xA;&#xA;  auto task = std::make_shared&amp;lt; std::packaged_task&amp;lt;return_type()&amp;gt; &amp;gt;(&#xA;      std::bind(std::forward&amp;lt;F&amp;gt;(f), std::forward&amp;lt;Args&amp;gt;(args)...)&#xA;    );&#xA;      &#xA;  std::future&amp;lt;return_type&amp;gt; res = task-&amp;gt;get_future();&#xA;  {&#xA;    std::unique_lock&amp;lt;std::mutex&amp;gt; lock(queue_mutex);&#xA;&#xA;    // don&amp;#39;t allow enqueueing after stopping the pool&#xA;    if(stop)&#xA;      throw std::runtime_error(&amp;#34;enqueue on stopped ThreadPool&amp;#34;);&#xA;&#xA;    tasks.emplace([task](){ (*task)(); });&#xA;  }&#xA;  condition.notify_one();&#xA;  return res;&#xA;}&#xA;&#xA;void DataProcessing(std::string&amp;amp; data, const std::string&amp;amp; operation) {&#xA;  data += operation;&#xA;  std::this_thread::sleep_for(std::chrono::seconds(1));&#xA;}&#xA;&#xA;int main(int argc, char** argv) {&#xA;  ThreadPool thread_pool(4);&#xA;  std::string data = &amp;#34;raw&amp;#34;;&#xA;  auto time1 = std::chrono::system_clock::now();&#xA;  std::vector&amp;lt;std::future&amp;lt;void&amp;gt;&amp;gt; futures;&#xA;  for (int i = 0; i &amp;lt; 4; ++i) {&#xA;    futures.emplace_back(thread_pool.enqueue(std::bind(DataProcessing, std::ref(data), std::to_string(i))));&#xA;  }&#xA;  for (auto&amp;amp; future : futures) {&#xA;    try {&#xA;      if (future.valid()) {&#xA;        future.get();&#xA;      } else {&#xA;        std::cerr &amp;lt;&amp;lt; &amp;#34;Future is invalid&amp;#34;;&#xA;      }&#xA;    } catch (const std::future_error&amp;amp; ex) {&#xA;      std::cerr &amp;lt;&amp;lt; &amp;#34;Caught a future error with code[&amp;#34; &amp;lt;&amp;lt; ex.code() &amp;lt;&amp;lt; &amp;#34;] message[&amp;#34; &amp;lt;&amp;lt; ex.what() &amp;lt;&amp;lt; &amp;#34;].&amp;#34;;&#xA;      throw ex;&#xA;    }&#xA;  }&#xA;  auto time2 = std::chrono::system_clock::now();&#xA;  std::chrono::duration&amp;lt;double&amp;gt; diff = time2 - time1;&#xA;  std::cout &amp;lt;&amp;lt; data &amp;lt;&amp;lt; std::endl;&#xA;  std::cout &amp;lt;&amp;lt; &amp;#34;Time Diff = &amp;#34; &amp;lt;&amp;lt; diff.count() * 1000&amp;lt;&amp;lt; &amp;#34; msec.&amp;#34; &amp;lt;&amp;lt; std::endl;&#xA;  return 0;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;</description>
        </item></channel>
</rss>
