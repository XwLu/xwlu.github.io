<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
    <channel>
        <title>Class Abstract on 朝花夕拾</title>
        <link>https://example.org/post/coding/c&#43;&#43;/class/</link>
        <description>Recent content in Class Abstract on 朝花夕拾</description>
        <generator>Hugo -- gohugo.io</generator>
        <language>en-us</language>
        <lastBuildDate>Wed, 02 Apr 2025 10:32:39 +0800</lastBuildDate><atom:link href="https://example.org/post/coding/c++/class/index.xml" rel="self" type="application/rss+xml" /><item>
            <title>Class Access &amp; Friend</title>
            <link>https://example.org/post/coding/c&#43;&#43;/class/access-friend/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/class/access-friend/</guid>
            <description>&lt;h1 id=&#34;访问限定符与友元&#34;&gt;访问限定符与友元&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;使用public/private/protected限定类成员的访问权限&#xA;&lt;ul&gt;&#xA;&lt;li&gt;类与结构体缺省访问权限的区别：类默认是private，结构体默认是public&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;使用友元打破访问权限限制——关键字friend&#xA;&lt;ul&gt;&#xA;&lt;li&gt;&#xA;&lt;p&gt;声明某个类或者某个函数是当前类的友元——慎用！&lt;/p&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;&#xA;&lt;p&gt;friend声明放在哪都行(public/private/protected都可以)&lt;/p&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;&#xA;&lt;p&gt;在类内首次声明友元类或者友元函数（下面代码中最开头的“声明函数或类”部分可以删掉，可以直接把Str类中的“friend int main();”视作main函数的声明）&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;注意使用限定名称引入友元并非友元类（友元函数）的声明&#xA;&lt;ul&gt;&#xA;&lt;li&gt;比如把Str中的代码改为“friend int ::main();”，编译就不通过了&lt;/li&gt;&#xA;&lt;li&gt;因为::main()不能被解析为函数的声明，而::main()函数在这之前又没有声明，所以就无法通过编译了&lt;/li&gt;&#xA;&lt;li&gt;解决方案就是在最前面加上“void main();”，或者把“friend int ::main();”改回“friend int main();”&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;// 声明函数或类&#xA;int main();&#xA;class Str2;&#xA;&#xA;class Str {&#xA;  // 声明main函数和Str2类是当前类的友元&#xA;  friend int main();&#xA;  friend Str2;  // friend class Str2也可以&#xA;&#xA;  // 默认是private&#xA;  inline static int x;&#xA;};&#xA;&#xA;class Str2 {&#xA;  void fun() {&#xA;    std::cout &amp;lt;&amp;lt; Str::x &amp;lt;&amp;lt; std::endl;  // 合法访问&#xA;  }&#xA;};&#xA;&#xA;int main() {&#xA;  std::cout &amp;lt;&amp;lt; Str::x &amp;lt;&amp;lt; std::endl;  // 合法访问，声明了友元&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;&#xA;&lt;p&gt;友元函数的类内类外定义（注意：友元类不可以类内定义）&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;类外定义&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;// 类外定义&#xA;class Str {&#xA;  inline static int x;&#xA;  int y;&#xA;  friend void fun();&#xA;};&#xA;&#xA;void fun() {&#xA;  Str val;&#xA;  std::cout &amp;lt;&amp;lt; val.y &amp;lt;&amp;lt; std::endl;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;类内定义（隐藏友元hidden friend）&#xA;&lt;ul&gt;&#xA;&lt;li&gt;错误示例&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;// 类内定义&#xA;class Str {&#xA;  inline static int x;&#xA;  int y;&#xA;&#xA;  // fun函数不是Str的成员函数，它是Str友元函数，所以fun的作用域是Str外部的全局域&#xA;  // 但是fun函数在外部又无法调用，因为fun函数的定义在Str内部，有定义而无声明&#xA;  friend void fun() {&#xA;    Str val;&#xA;    std::cout &amp;lt;&amp;lt; val.y &amp;lt;&amp;lt; std::endl;&#xA;  }&#xA;};&#xA;&#xA;int main() {&#xA;  fun();  // 编译失败，常规的名称查找是查找不到fun的&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;隐藏友元的正确打开方式&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;// 常规的名称查找找不到fun，Argument-Depende-Lookup(ADL)可以）&#xA;&#xA;// 类内定义&#xA;class Str {&#xA;  inline static int x;&#xA;  int y;&#xA;&#xA;  friend void fun2(const Str&amp;amp; val) {&#xA;    std::cout &amp;lt;&amp;lt; val.y &amp;lt;&amp;lt; std::endl;&#xA;  }&#xA;};&#xA;&#xA;int main() {&#xA;  Str val;&#xA;  fun2(val);  // 编译成功&#xA;  // 我们在调用fun2的时候传入了val，其类型是Str&#xA;  // 所以编译器在执行这条语句的时候除了常规的名称查找，还会进行实参类型的依赖查找&#xA;  // 就会扫描Str内部的内容，就会找到fun2&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
        </item><item>
            <title>Class Basis</title>
            <link>https://example.org/post/coding/c&#43;&#43;/class/basis/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/class/basis/</guid>
            <description>&lt;h1 id=&#34;结构体&#34;&gt;结构体&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;仅有声明的结构体是不完整类型&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str;&#xA;int main() {&#xA;  Str s;  // 编译不通过，只有声明没有定义，不知道Str的内部结构，占多大内存&#xA;  Str* s;  // 编译通过，指针的内存大小是固定的，不需要知道Str的内部结构和大小&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;结构体（以及类）的一处定义原则：翻译单元级别&#xA;&lt;ul&gt;&#xA;&lt;li&gt;下面这份代码是可以通过编译并运行的，两个相同的结构体在不同的编译单元&lt;/li&gt;&#xA;&lt;li&gt;source.cpp的内容&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str{&#xA;  int x;&#xA;};&#xA;&#xA;void fun() {&#xA;  Str m_str;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;main.cpp的内容&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  int x;&#xA;};&#xA;&#xA;void fun();&#xA;&#xA;int main() {&#xA;  fun();&#xA;  Str str;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;h1 id=&#34;数据成员的声明与初始化&#34;&gt;数据成员的声明与初始化&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;(C++11)数据成员可以使用decltype来声明其类型，但不能使用auto&lt;/li&gt;&#xA;&lt;li&gt;数据成员的声明可以引入const、引用、指针等限定&lt;/li&gt;&#xA;&lt;li&gt;数据成员会在构造对象时定义&lt;/li&gt;&#xA;&lt;li&gt;(C++11)支持类内成员初始化&lt;/li&gt;&#xA;&lt;li&gt;结构体支持聚合初始化，但不建议使用&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  int x;&#xA;  int y;&#xA;};&#xA;int main() {&#xA;  Str s{3, 4};&#xA;  // 上面的代码看起来没啥问题，但是万一哪天，结构体中的成员的顺序被移动了，或者x和y中间插入了一个新的成员，那么这里的聚合初始化都可能引发不想要的错误&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;为了解决上面的问题，C++20引入了指派初始化&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Str s{.x=3, .y=4};&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;hr&gt;&#xA;&lt;h1 id=&#34;mutable限定符&#34;&gt;mutable限定符&#xA;&lt;/h1&gt;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  mutable int x;&#xA;  int y;&#xA;};&#xA;int main() {&#xA;  const Str s;&#xA;  s.x = 3;  // 编译通过&#xA;  s.y = 4;  // 编译不通过&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;hr&gt;&#xA;&lt;h1 id=&#34;静态数据成员&#34;&gt;静态数据成员&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;&#xA;&lt;p&gt;多个对象之前共享的数据成员&lt;/p&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;&#xA;&lt;p&gt;定义方式的演化&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;C++98：1. 类外定义 2. const静态成员的类内初始化&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  static int x;&#xA;  int y;&#xA;&#xA;  const static array_size = 100;  // 如果没有使用const static，就无法定义Str，因为编译器不知道要分配多大的内存给对象&#xA;  int buffer[array_size];  // C++98会把这里的array_size直接替换成100&#xA;};&#xA;&#xA;int Str::x;  // 这里的定义不能少&#xA;&#xA;int main() {&#xA;  Str s1;&#xA;  Str s2;&#xA;  s1.x = 100;&#xA;  s2.x = 1;&#xA;  std::cout &amp;lt;&amp;lt; s1.x &amp;lt;&amp;lt; std::endl;  // 1&#xA;&#xA;  std::cout &amp;lt;&amp;lt; &amp;amp;(s1.array_size) &amp;lt;&amp;lt; std::endl;  // 编译不通过，因为C98标准的编译器直接把用到array_size的地方替换为100了，并没有真的分配一块内存来存放array_size&#xA;  // 如果一定要给它分配一个地址的话，需要在某个地方加一行定义：int Str::array_size;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;C++17：内联静态成员的初始化&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  inline static int array_size = 100;  // 不需要const了，可修改&#xA;  int buffer[array_size];&#xA;};&#xA;&#xA;std::cout &amp;lt;&amp;lt; &amp;amp;(s1.array_size) &amp;lt;&amp;lt; std::endl;  // 编译通过&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;&#xA;&lt;p&gt;静态数据成员可以使用auto推导类型&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  inline static auto array_size = 100;&#xA;};&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;&#xA;&lt;p&gt;静态数据成员的访问&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;str.x&lt;/li&gt;&#xA;&lt;li&gt;str-&amp;gt;x&lt;/li&gt;&#xA;&lt;li&gt;Str::x&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;&#xA;&lt;p&gt;在类的内部声明相同类型的静态数据成员&lt;/p&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  Str s;  // 编译不通过，因为不知道要给Str分配多大内存，又是鸡蛋问题&#xA;  static Str s;  // 编译通过，因为它被所有的Str对象共享，所以它不属于任何一个对象，所以在构建Str对象的时候，不用考虑它所占的内存&#xA;};&#xA;&#xA;Str Str::s;  // 如果没有这一行，编译能通过，但是link不行，因为s没有被定义，就没有对应的内存 &#xA;&#xA;int main() {&#xA;  Str s1;&#xA;  Str s2;&#xA;  std::cout &amp;lt;&amp;lt; &amp;amp;(s1.s) &amp;lt;&amp;lt; std::endl;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;&#xA;&lt;p&gt;inline使用的注意点&lt;/p&gt;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;错误使用方法&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  inline static int x;  // 编译通过&#xA;  inline static Str s;  // 编译不通过，因为使用inline后，这里就不是声明而是定义了，但程序执行到这一行的时候Str还是不完全类型，所以无法完成定义的操作&#xA;};&#xA;&#xA;int main() {&#xA;  Str s1;&#xA;  Str s2;&#xA;  std::cout &amp;lt;&amp;lt; &amp;amp;(s1.s) &amp;lt;&amp;lt; std::endl;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;正确使用方法&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  static Str s;&#xA;};&#xA;&#xA;inline static Str::s;   &#xA;&#xA;int main() {&#xA;  Str s1;&#xA;  Str s2;&#xA;  std::cout &amp;lt;&amp;lt; &amp;amp;(s1.s) &amp;lt;&amp;lt; std::endl;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;hr&gt;&#xA;&lt;h1 id=&#34;成员函数&#34;&gt;成员函数&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;声明与定义&#xA;&lt;ul&gt;&#xA;&lt;li&gt;类内定义(隐式内联)&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;// str.h&#xA;struct Str {&#xA;  int x;&#xA;  void fun() {}&#xA;};&#xA;&#xA;// main.cc&#xA;#include &amp;#34;str.h&amp;#34;&#xA;&#xA;//main2.cc&#xA;#include &amp;#34;str.h&amp;#34;&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;类内声明+类外定义&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;// str.h&#xA;struct Str {&#xA;  int x;&#xA;  void fun();&#xA;};&#xA;&#xA;// str.cc&#xA;void Str::fun() {}&#xA;&#xA;// main.cc&#xA;#include &amp;#34;str.h&amp;#34;&#xA;&#xA;// main2.cc&#xA;#include &amp;#34;str.h&amp;#34;&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;&#xA;&lt;li&gt;错误示范&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;// str.h&#xA;struct Str {&#xA;  int x;&#xA;  void fun();&#xA;};&#xA;void Str::fun() {}  // 报错，重复定义，不能简单将类外定义实现在头文件里，除非改成下面一行&#xA;inline void Str::fun() {}  // 通过，内联函数（也可以直接把fun的定义写在类内，隐式内联）&#xA;&#xA;// main.cc&#xA;#include &amp;#34;str.h&amp;#34;&#xA;&#xA;// main2.cc&#xA;#include &amp;#34;str.h&amp;#34;&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;类与编译期的两遍处理&#xA;&lt;ul&gt;&#xA;&lt;li&gt;我们在定义类的时候，喜欢把成员函数放前面，成员变量放后面&lt;/li&gt;&#xA;&lt;li&gt;当某些成员函数采用类内定义的时候，它用到的成员函数的定义还在后面&lt;/li&gt;&#xA;&lt;li&gt;所以C++引入了两遍处理逻辑，看到类内定义的时候，只当作声明，等到把类的声明都过一遍之后再回来处理隐式内联函数的定义&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;成员函数与尾随返回类型(trail returning type)&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;// str.h&#xA;struct Str {&#xA;  using MyRes = int;&#xA;  MyRes fun();&#xA;  int x;&#xA;};&#xA;&#xA;// str.cc&#xA;#include &amp;#34;str.h&amp;#34;&#xA;MyRes Str::fun() {  // 错误，MyRes是属于Str类域的，需要改成Str::MyRes&#xA;  return x;&#xA;}&#xA;&#xA;auto Str::fun() -&amp;gt; MyRes {  // 合法，因为编译器从前面的Str::已经知道我们在处理Str类内的成员函数了，因此后面的MyRes就会从Str类域里去找定义了&#xA;  return x;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;成员函数与this指针&#xA;&lt;ul&gt;&#xA;&lt;li&gt;每一个成员函数被调用的时候，编译器都会隐式地传一个this指针进去，用于确定函数里的成员变量是属于哪个对象的。&lt;/li&gt;&#xA;&lt;li&gt;基于const的成员函数重载&#xA;&lt;ul&gt;&#xA;&lt;li&gt;this是被const修饰的，所以你不能执行this = nullptr这种，但是你可以操作this指向的内容&lt;/li&gt;&#xA;&lt;li&gt;如果在成员函数的末尾加上const，传入的参数就变成const Str* const this，此时this和它指向的内容都无法被修改&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  // 下面这两个函数能够形成重载，因为传入的this指针类型不一样&#xA;  void fun(int x);  // Str* const this&#xA;  void fun(int x) const;  // const Str* const this&#xA;};&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;成员函数的名称查找与隐藏关系&#xA;&lt;ul&gt;&#xA;&lt;li&gt;函数内部（包括形参名称）隐藏函数外部&lt;/li&gt;&#xA;&lt;li&gt;类内部名称隐藏类外部名称&lt;/li&gt;&#xA;&lt;li&gt;使用this或域操作符引入依赖型名称查找(::x表示全局变量x)&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;静态成员函数&#xA;&lt;ul&gt;&#xA;&lt;li&gt;用于描述与类紧密相关，但又不需要一个对象实例就可以调用的函数&lt;/li&gt;&#xA;&lt;li&gt;被所有该类型对象所共享&lt;/li&gt;&#xA;&lt;li&gt;在调用该函数的时候，不会隐式传入this指针了&lt;/li&gt;&#xA;&lt;li&gt;因为上一条特性，所以它不能调用一般成员变量，但可以返回静态的数据成员&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;成员函数基于引用限定符的重载(C++11)&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;class Type {&#xA;  void foo() &amp;amp;;&#xA;  void foo() &amp;amp;&amp;amp;;&#xA;  void foo() const &amp;amp;;&#xA;  void foo() const &amp;amp;&amp;amp;;&#xA;};&#xA;&#xA;int main() {&#xA;  Type obj;&#xA;  obj.foo();  // void foo() &amp;amp;;&#xA;  Type().foo();  // void foo() &amp;amp;&amp;amp;;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
        </item><item>
            <title>Constexpr of Class</title>
            <link>https://example.org/post/coding/c&#43;&#43;/class/constexpr/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/class/constexpr/</guid>
            <description>&lt;h1 id=&#34;字面值类&#34;&gt;字面值类&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;可以构造编译期常量的类型&lt;/li&gt;&#xA;&lt;li&gt;其数据成员需要是字面值类型(string就不可以，因为string不是字面值常量)&lt;/li&gt;&#xA;&lt;li&gt;提供constexpr/consteval构造函数&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;class Str {&#xA;public:&#xA;  constexpr Str(int val) : x(val) {}  // constexpr表示该值既可以在编译期调用也可以在运行期调用&#xA;private:&#xA;  int x;&#xA;};  &#xA;&#xA;int main() {&#xA;  constexpr Str a(3);  // Str的构造函数一定要加constexpr修饰，否则报错&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;&#xA;&lt;li&gt;小心使用consteval&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;class Str {&#xA;public:&#xA;  consteval Str(int val) : x(val) {}  // consteval表示该值可以在编译期调用&#xA;private:&#xA;  int x;&#xA;};&#xA;&#xA;int main() {&#xA;  int x;&#xA;  Str b(x);  // 在低版本的编译器里可能不会报错，但是高版本的编译器会报错，导致同一份代码的编译结果不一致&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;提供constexpr和consteval成员函数（小心使用consteval）&lt;/li&gt;&#xA;&lt;li&gt;注意：从C++14起，constexpr/consteval成员函数非const成员函数&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
        </item><item>
            <title>Construction of Class</title>
            <link>https://example.org/post/coding/c&#43;&#43;/class/construct/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/class/construct/</guid>
            <description>&lt;h1 id=&#34;构造函数&#34;&gt;构造函数&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;代理构造函数(C++11)&lt;/li&gt;&#xA;&lt;li&gt;初始化列表&#xA;&lt;ul&gt;&#xA;&lt;li&gt;提高代码性能&lt;/li&gt;&#xA;&lt;li&gt;类中的引用成员必须使用初始化列表进行初始化&lt;/li&gt;&#xA;&lt;li&gt;类成员的初始化顺序与声明的顺序一致，与其在初始化列表中的顺序无关&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;class Str {&#xA;public:&#xA;  Str(size_t input):y(input), x(y + 1) {&#xA;    std::cout &amp;lt;&amp;lt; x &amp;lt;&amp;lt; &amp;#34; &amp;#34; &amp;lt;&amp;lt; y &amp;lt;&amp;lt; std::endl;&#xA;  }&#xA;private:&#xA;  size_t x;&#xA;  size_t y;&#xA;};&#xA;// 打印结果是：4199057 4&#xA;// 因为x和y的初始化顺序是先初始化x，再初始化y，而初始化x时，y还没被初始化，所以y+1的结果是个随机值&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;使用初始化列表可以覆盖类内成员初始化的行为&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;缺省构造函数：不需要提供实际参数就可以调用的构造函数&#xA;&lt;ul&gt;&#xA;&lt;li&gt;如果类内没有提供任何构造函数，在条件允许的情况下，编译器会合成一个缺省构造函数&#xA;&lt;ul&gt;&#xA;&lt;li&gt;如果类内有构造函数，编译器就不会合成缺省构造函数了&lt;/li&gt;&#xA;&lt;li&gt;如果类内有引用成员变量，编译器也不会合成缺省构造函数&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;调用缺省构造函数时避免most vexing parse&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struc Str {&#xA;  int x;&#xA;};&#xA;int main() {&#xA;  Str m;  // 合法&#xA;  Str m();  // 不合法，编译器会把这行当作一个函数声明&#xA;  Str m{};  // 合法&#xA;  std::cout &amp;lt;&amp;lt; m.x &amp;lt;&amp;lt; std::endl;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;使用default关键字定义缺省构造函数&#xA;&lt;ul&gt;&#xA;&lt;li&gt;当我们在类内定义了一个带参数的构造函数后，编译器不会自动合成缺省构造函数了，这时候我们可以用Str() = default;来自己构造一个&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;单一构造函数&#xA;&lt;ul&gt;&#xA;&lt;li&gt;可以是为一种类型转换函数&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;public:&#xA;  Str(int x) : val(x) {}&#xA;private: &#xA;  int val;&#xA;};&#xA;&#xA;void fun(Str m) {}&#xA;&#xA;int main() {&#xA;  Str m(3);  // 合法&#xA;  Str m{3};  // 合法&#xA;  Str m = 3;  // 合法&#xA;  fun(3);  // 合法，发生了隐式类型转换 &#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;explict关键字避免求值过程中的隐式类型转换&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;public:&#xA;  explict Str(int x) : val(x) {}&#xA;private: &#xA;  int val;&#xA;};&#xA;&#xA;void fun(Str m) {}&#xA;&#xA;int main() {&#xA;  Str m(3);  // 合法&#xA;  Str m{3};  // 合法&#xA;  Str m = 3;  // 不合法，explict起作用了&#xA;  fun(3);  // 不合法，explict起作用了&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;拷贝构造函数&#xA;&lt;ul&gt;&#xA;&lt;li&gt;会在涉及到拷贝初始化的场景被调用，比如：参数传递。因此需要注意拷贝构造函数的形参类型&#xA;&lt;ul&gt;&#xA;&lt;li&gt;拷贝构造函数需要传入引用，最好再加上const&lt;/li&gt;&#xA;&lt;li&gt;如果不传引用，首先需要将实参拷贝给形参，就需要调用拷贝构造函数，而拷贝构造函数正在被定义中，就无限嵌套了&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;如果未显式提供，编译器会自动合成一个，合成版本会依次对每个数据成员调用拷贝构造函数，因此拷贝构造函数也可以使用default来构造&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;移动构造函数(C++11)：接受一个当前类右值引用对象的构造函数&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;std::string ori(&amp;#34;abc&amp;#34;);&#xA;std::string new_str = ori;  // 拷贝构造函数&#xA;std::cout &amp;lt;&amp;lt; ori &amp;lt;&amp;lt; &amp;#34; &amp;#34; &amp;lt;&amp;lt; new_str &amp;lt;&amp;lt; std::endl;  // abc abc&#xA;&#xA;std::string new_str2 = std::move(ori);  // 移动构造函数&#xA;std::cout &amp;lt;&amp;lt; ori &amp;lt;&amp;lt; &amp;#34; &amp;#34; &amp;lt;&amp;lt; new_str2 &amp;lt;&amp;lt; std::endl;  //  abc&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;&#xA;&lt;li&gt;自定义移动构造函数&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struc Str {&#xA;  // 构造函数&#xA;  Str() = default;&#xA;  Str(const Str&amp;amp;) = default;&#xA;  Str(Str&amp;amp;&amp;amp; x) : val(x.val), a(std::move(x.a)) {};&#xA;&#xA;  // void fun() {std::cout &amp;lt;&amp;lt; val &amp;lt;&amp;lt; &amp;#34; &amp;#34; &amp;lt;&amp;lt; a &amp;lt;&amp;lt; std::endl;}&#xA;  int val = 3;&#xA;  std::string a = &amp;#34;abc&amp;#34;;&#xA;};&#xA;&#xA;int main() {&#xA;  Str m;&#xA;  m.fun();  // 3 abc &#xA;  Str m2 = std::move(m);&#xA;  m.fun();  // 3&#xA;  m2.fun();  // 3 abc&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;缺省移动构造函数&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str2 {&#xA;  Str2(const Str2&amp;amp;);&#xA;};&#xA;&#xA;struct Str {&#xA;  // 缺省构造&#xA;  Str() = default;&#xA;  // 拷贝构造&#xA;  Str(const Str&amp;amp;) = default;&#xA;  // 移动构造&#xA;  Str(Str&amp;amp;&amp;amp;) = default;&#xA;  // 拷贝赋值&#xA;  Str&amp;amp; operator= (const Str&amp;amp; x) {&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;copy assignment is called&amp;#34; &amp;lt;&amp;lt; std::endl;&#xA;    val = x.val;&#xA;    a = x.a;&#xA;    return *this;&#xA;  }&#xA;  // 移动赋值&#xA;  Str&amp;amp; operator= (Str&amp;amp;&amp;amp; x) {&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;move assignment is called&amp;#34; &amp;lt;&amp;lt; std::endl;&#xA;    val = std::move(x.val);&#xA;    a = std::move(x.a);&#xA;    return *this;&#xA;  }&#xA;&#xA;  int val;&#xA;  std::string a;&#xA;  Str2 m_str2;&#xA;};&#xA;&#xA;int main() {&#xA;  Str m;  // 编译失败&#xA;  // 因为Str2有拷贝构造函数了，编译器不会自动合成缺省构造函数，而Str的default构造函数又需要调用Str2的缺省构造函数，所以就报错了&#xA;  // 解决方案：在Str2类内加入&amp;#34;Str2() = default;&amp;#34;&#xA;&#xA;  Str m2 = std::move(m);  //移动构造&#xA;  // 这里会依次对Str类的成员变量调用移动构造，由于Str2没有定义移动构造，所以会执行Str2的拷贝构造&#xA;&#xA;  Str m3 = m;  // 拷贝构造&#xA;&#xA;  Str m4;&#xA;  m4 = m;  // 拷贝赋值&#xA;  m4 = std::move(m);  // 移动赋值&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;移动构造函数通常声明为不可抛出异常函数：在函数声明的后面加上&amp;quot;noexcept&amp;quot;&lt;/li&gt;&#xA;&lt;li&gt;注意右值引用对象用作表达式时是左值&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;void fun(Str&amp;amp;&amp;amp; x) {&#xA;  std::cout &amp;lt;&amp;lt; x.val &amp;lt;&amp;lt; std::endl;  // 在这一行里面，x是个左值&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;拷贝赋值函数(operator=)&#xA;&lt;ul&gt;&#xA;&lt;li&gt;代码如上&lt;/li&gt;&#xA;&lt;li&gt;拷贝赋值不可以使用初始化列表&lt;/li&gt;&#xA;&lt;li&gt;拷贝赋值函数通常返回该类型的引用&#xA;&lt;ul&gt;&#xA;&lt;li&gt;目的是为了支持&amp;quot;m1=m2=m3;&amp;ldquo;的使用方式&lt;/li&gt;&#xA;&lt;li&gt;也可以返回void，比如把上面的&amp;quot;return *this&amp;quot;去掉，这样也可以赋值，但是不支持连等操作了&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;一些情况下编译器会自动合成&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;移动赋值函数&#xA;&lt;ul&gt;&#xA;&lt;li&gt;不可以使用初始化列表&lt;/li&gt;&#xA;&lt;li&gt;通常返回该类型的引用&lt;/li&gt;&#xA;&lt;li&gt;注意给自身赋值的情况&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  Str&amp;amp; operator= (Str&amp;amp;&amp;amp; x) {&#xA;    if (&amp;amp;x == this) {&#xA;      return *this;&#xA;    }&#xA;    delete ptr;&#xA;    ptr = x.ptr;&#xA;    x.ptr = nullptr;&#xA;  }&#xA;&#xA;  int* ptr;&#xA;};&#xA;&#xA;int main() {&#xA;  Str m1;&#xA;  m1 = std::move(m1);  // 如果没有一开始的if判断，这里会把m1的指针给搞成nullptr，显然不是我们想要的&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;一些情况下编译器会自动合成&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;hr&gt;&#xA;&lt;h1 id=&#34;析构函数&#34;&gt;析构函数&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;无参数，无返回值，用于释放内存&lt;/li&gt;&#xA;&lt;li&gt;内存回收是在析构函数执行完才进行&lt;/li&gt;&#xA;&lt;li&gt;除非显示声明，否则编译器会自动合成一个，其内部逻辑为平凡的&lt;/li&gt;&#xA;&lt;li&gt;析构函数通常不能抛出异常&#xA;&lt;ul&gt;&#xA;&lt;li&gt;因为C++在抛出异常的时候会把当前域内的数据都析构掉，如果析构过程又有新的异常，C++会直接退出，因为C++无法处理多个异常同时抛出的情况&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;~Str() noexcept = default;&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;hr&gt;&#xA;&lt;h1 id=&#34;相关注意点&#34;&gt;相关注意点&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;通常来说，一个类&#xA;&lt;ul&gt;&#xA;&lt;li&gt;如果需要定义析构函数，那么也需要定义拷贝构造和拷贝赋值函数&lt;/li&gt;&#xA;&lt;li&gt;如果需要定义拷贝构造函数，那么也需要定义拷贝赋值函数&lt;/li&gt;&#xA;&lt;li&gt;如果需要定义拷贝构造（赋值）函数，那么也需要定义移动构造（赋值）函数&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;default关键字&#xA;&lt;ul&gt;&#xA;&lt;li&gt;只对特殊成员函数有效（比如你只能对构造函数、析构函数等使用default方法）&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;delete关键字&#xA;&lt;ul&gt;&#xA;&lt;li&gt;对所有函数都有效&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;void fun(int) = delete;&#xA;void fun(double) { std::cout &amp;lt;&amp;lt; &amp;#34;double is called&amp;#34;; }&#xA;&#xA;class Str {&#xA;public:&#xA;  Str() = default;&#xA;  ~Str() = delete;&#xA;private:&#xA;  int* ptr;&#xA;};&#xA;&#xA;int main() {&#xA;  fun(3);  // 程序报错，因为根据名称查找规则，这里会调用void fun(int)，但是该函数是delete修饰的，不能被调用&#xA;  Str a;  // 程序报错，因为变量a在main函数执行结束后会被销毁，但析构函数是delete修饰的，不能被调用&#xA;  Str* p = new Str();  // 合法，因为new对象不会被自动销毁，所以不会调用析构函数，但是这么写是有内存泄漏的&#xA;  // 同时，如果主动调用delete p;，也无法通过编译&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;注意，不要为移动构造（赋值）函数引入delete限定符&#xA;&lt;ul&gt;&#xA;&lt;li&gt;如果只允许拷贝，那就引入拷贝构造即可&lt;/li&gt;&#xA;&lt;li&gt;如果不需要拷贝，那就将拷贝构造声明为delete即可&lt;/li&gt;&#xA;&lt;li&gt;注意delete移动构造（赋值）对C++17的新影响&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;class Str {&#xA;public:&#xA;  Str() = default;&#xA;  Str(const Str&amp;amp; val) = default;&#xA;  Str(Str&amp;amp;&amp;amp; val) = delete;&#xA;};&#xA;&#xA;voidf fun(Str val) {}&#xA;&#xA;int main() {&#xA;  fun(Str{});  // C++11会报错，因为传入了一个将亡值，会调用移动构造函数&#xA;  // C++17不会报错，因为C++17会优化掉移动构造这一步，这就导致同一份代码在不同环境下表现不一样了&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;特殊成员的合成行为列表（红色的表示支持但可能会废除的行为）&#xA;&lt;ul&gt;&#xA;&lt;li&gt;&lt;img alt=&#34;special-members&#34; loading=&#34;lazy&#34; sizes=&#34;(max-width: 767px) calc(100vw - 30px), (max-width: 1023px) 700px, (max-width: 1279px) 950px, 1232px&#34; src=&#34;../special-members.png&#34;&gt;&lt;/li&gt;&#xA;&lt;li&gt;横着看，最左侧一栏表示如果用户声明了xxx，右边的表示系统的行为。&lt;/li&gt;&#xA;&lt;li&gt;2014年的标准&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
        </item><item>
            <title>Inheritance of Class</title>
            <link>https://example.org/post/coding/c&#43;&#43;/class/inheritance/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/class/inheritance/</guid>
            <description>&lt;h1 id=&#34;类的继承&#34;&gt;类的继承&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;通过类的继承（派生）来引入“是一个”的关系&#xA;&lt;ul&gt;&#xA;&lt;li&gt;通常采用public继承&#xA;&lt;ul&gt;&#xA;&lt;li&gt;class缺省情况下采用的是private继承&lt;/li&gt;&#xA;&lt;li&gt;struct缺省情况下采用的是public继承&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;继承部分不是类的声明，声明的时候直接&amp;quot;class xxx;&amp;ldquo;即可，不可以在声明的时候带上&amp;rdquo;: public base&amp;quot;&lt;/li&gt;&#xA;&lt;li&gt;使用基类的指针或引用可以指向派生类对象&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {};&#xA;&#xA;struct Derived : public Base {};&#xA;&#xA;int main () {&#xA;  Derived d;&#xA;  Base&amp;amp; ref = d;&#xA;  Base* ptr = &amp;amp;d;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;静态类型 v.s. 动态类型&#xA;&lt;ul&gt;&#xA;&lt;li&gt;静态类型是编译期确定的变量类型，比如上面的ref静态类型是Base&amp;amp;，ptr静态类型是Base*&lt;/li&gt;&#xA;&lt;li&gt;动态类型是运行期变量实际被赋予的类型，比如上面的ref动态类型是Derived&amp;amp;，ptr静态类型是Derived*&lt;/li&gt;&#xA;&lt;li&gt;静态类型是给编译器看的，所以变量只能调用静态类型所拥有的成员变量或成员函数&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;protected限定符：派生类可访问，外部不可访问&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;类的派生会形成嵌套域&#xA;&lt;ul&gt;&#xA;&lt;li&gt;派生类所在域位于基类内部&lt;/li&gt;&#xA;&lt;li&gt;派生类中的名称定义会覆盖基类&lt;/li&gt;&#xA;&lt;li&gt;使用域操作符显示访问基类成员&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  int val = 2;&#xA;};&#xA;&#xA;class Derived : public Base {&#xA;public:&#xA;  void fun() {&#xA;    std::cout &amp;lt;&amp;lt; val &amp;lt;&amp;lt; std::endl;  // 3, val的值被Derived中的val覆盖&#xA;    std::cout &amp;lt;&amp;lt; Base::val &amp;lt;&amp;lt; std::endl;  // 2, 限定了Base域之后，就会打印Base中val的值&#xA;  }&#xA;&#xA;  int val = 3;&#xA;};&#xA;&#xA;int main() {&#xA;  Derived d;&#xA;  d.fun();&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;在派生类中调用基类的构造函数&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  Base(int) {}&#xA;};&#xA;&#xA;class Derived : public Base {&#xA;  Derived(int a) : Base(a) {}  // 必须要在初始化列表中调用Base的初始化函数，如果放在后面的函数体中是无法通过编译的&#xA;}&#xA;&#xA;int main() {&#xA;  Derived d(3);&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;虚函数&#xA;&lt;ul&gt;&#xA;&lt;li&gt;通过虚函数与引用（指针）实现动态绑定&#xA;&lt;ul&gt;&#xA;&lt;li&gt;使用关键字virtual引入&lt;/li&gt;&#xA;&lt;li&gt;非静态、非构造函数可以声明为虚函数&lt;/li&gt;&#xA;&lt;li&gt;虚函数会引入vtable结构&#xA;&lt;ul&gt;&#xA;&lt;li&gt;dynamic_cast&lt;/li&gt;&#xA;&lt;li&gt;&lt;img alt=&#34;vtable&#34; loading=&#34;lazy&#34; sizes=&#34;(max-width: 767px) calc(100vw - 30px), (max-width: 1023px) 700px, (max-width: 1279px) 950px, 1232px&#34; src=&#34;../vtable.png&#34;&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  virtual void baseMethod() {}&#xA;  int baseMember;&#xA;};&#xA;&#xA;class myClassDerived : public Base {&#xA;  virtual void derivedMethod() {}&#xA;  int derivedMember;&#xA;};&#xA;&#xA;class myClassDerived2 : public myClassDerived {&#xA;  virtual void derivedMethod2() {}&#xA;  int derivedMember2;&#xA;};&#xA;&#xA;int main() {&#xA;  myClassDerived2 d;&#xA;&#xA;  // 从派生类转换到基类很自然&#xA;  Base* ptr = &amp;amp;d;&#xA;  Base&amp;amp; ref = d;&#xA;&#xA;  // 从基类转换为派生类可能存在风险，安全的做法是使用dynamic_cast&#xA;  // 下面这段代码之所以可以编译并运行就是因为vtable里包含了typeinfo of myClassDerived2&#xA;  myClassDerived2&amp;amp; ref2 = dynamic_cast&amp;lt;myClassDerived2&amp;amp;&amp;gt;(ref);  // 如果ref的typeinfo中记录的确实是myClassDerived2类型的数据，转换就成功，否则这里会抛出异常&#xA;  myClassDerived2* ptr2 = dynamic_cast&amp;lt;myClassDerived2*&amp;gt;(ptr);  // 如果ptr的typeinfo中记录的确实是myClassDerived2类型的数据，转换就成功，否则这里会返回空指针 &#xA;  // 如果把上面的Base class中的虚函数注释掉，编译就会报错，因为vtable没了，typeinfo也就没了&#xA;  // 如果把ptr的类型从Base*变成myClassDerived*，那就要保证myClassDerived的虚函数存在，Base中的虚函数则无关紧要&#xA;  // 换句话说，从基类A转换到派生类B，需要保证A中存在虚函数&#xA;&#xA;  // 注意:下面的转换也是可以的，虽然ref和ptr指向的数据的类型是myClassDerived2，但是myClassDerived2是继承自myClassDerived，所以可以转换&#xA;  myClassDerived&amp;amp; ref3 = dynamic_cast&amp;lt;myClassDerived&amp;amp;&amp;gt;(ref);&#xA;  myClassDerived* ptr3 = dynamic_cast&amp;lt;myClassDerived*&amp;gt;(ptr);&#xA;&#xA;  // 从上面的例子可以看出，dynamic_cast的使用会占用很多动态过程的运算资源，所以在追求性能的场景下慎用&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;虚函数在基类中的定义&#xA;&lt;ul&gt;&#xA;&lt;li&gt;引入缺省逻辑&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  virtual void fun() {&#xA;    std::cout &amp;lt;&amp;lt; Base::fun &amp;lt;&amp;lt; std::endl;&#xA;  }&#xA;};&#xA;&#xA;class Derived : public Base {&#xA;  void fun() {&#xA;    std::cout &amp;lt;&amp;lt; Derived::fun &amp;lt;&amp;lt; std::endl;&#xA;  }    &#xA;}&#xA;&#xA;int main() {&#xA;  Derived d;&#xA;  d.fun();  // Derived::fun&#xA;&#xA;  Base&amp;amp; b = d;&#xA;  b.fun();  // Derived::fun&#xA;  // 虽然b的静态类型是Base&amp;amp;，但是b引用(指针)指向的数据内存里的vtable中的fun函数指针指向的是Derived::fun&#xA;  // 但是如果把上面的Base中的virtual关键字去掉，上面还是会打印Derived::fun（因为覆盖作用依然存在）；但是这里就会打印Base::fun，因为vtable不存在了，所有的函数调用会在编译期就被固定下来，而编译器只看静态类型（b的静态类型是Base&amp;amp;），所以b.fun就只会绑定到Base::fun&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;虚函数的意义就在于我们可以通过同一个Base的接口，通过传入不同派生类的对象，实现不同的代码逻辑，也就是&lt;strong&gt;动态多态&lt;/strong&gt;（区别于静态多态）&lt;/li&gt;&#xA;&lt;li&gt;可以通过=0声明纯虚函数，相应地构造抽象基类&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;虚函数在派生类中的重写&#xA;&lt;ul&gt;&#xA;&lt;li&gt;函数签名保持不变（唯一可以变的是：返回类型可以是原始返回指针/引用类型的派生指针/引用类型）&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {};&#xA;&#xA;struct Derived : public Base {};&#xA;&#xA;struct Base2 {&#xA;  virtual Base&amp;amp; fun() {&#xA;    static Base b;&#xA;    return b;&#xA;  }&#xA;};&#xA;&#xA;struct Derived2 : public Base2 {&#xA;  Derived&amp;amp; fun() {  // 通过，因为Derived继承自Base&#xA;    static Derived inst;&#xA;    return inst;&#xA;  }&#xA;};&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;纯虚函数可以被定义，且可以在派生类中调用基类的纯虚函数。&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  virtual void fun() = 0;&#xA;}&#xA;&#xA;void Base::fun() {&#xA;  std::cout &amp;lt;&amp;lt; &amp;#34;Base::fun&amp;#34; &amp;lt;&amp;lt; std::endl;  &#xA;  // 如果基类过于抽象，导致某些函数无法被完整定义，就可以声明为纯虚函数，强制派生类去重写。但是基类的纯虚函数仍然可以被定义，实现一些通用的预处理逻辑，在派生类中被调用。&#xA;}&#xA;&#xA;struct Derived : public Base {&#xA;  void fun() {&#xA;    Base::fun();  // 调用基类的纯虚函数&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;Derived::fun&amp;#34; &amp;lt;&amp;lt; std::endl;&#xA;  };  &#xA;};&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;虚函数特性保持不变&#xA;&lt;ul&gt;&#xA;&lt;li&gt;Base -&amp;gt; Derived -&amp;gt; Derived2，嵌套继承，Base中的虚函数fun在Derived中被重写后，依然是虚函数，所以Derived2再次重写fun后，还是会保留虚函数的特性&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;override关键字&#xA;&lt;ul&gt;&#xA;&lt;li&gt;让编译器检查是否基类中的虚函数确实被正确重写了&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;由虚函数所引入的动态绑定属于运行期行为，与编译期行为有所区别&#xA;&lt;ul&gt;&#xA;&lt;li&gt;虚函数的缺省实参只会考虑静态类型&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  virtual void fun(int x = 3) {&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;Base: &amp;#34; &amp;lt;&amp;lt; x &amp;lt;&amp;lt; std::endl;&#xA;  }&#xA;};&#xA;&#xA;struct Derived : public Base {&#xA;  void fun(int x = 4) override final {  // 这里final的含义是，后面继承自Derived的所有派生类都不会再重写fun函数&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;Derived: &amp;#34; &amp;lt;&amp;lt; x &amp;lt;&amp;lt; std::endl;&#xA;  }    &#xA;};&#xA;&#xA;struct Derived2 final : public Base {};  // 这里final的含义是，Derived2不会有派生类了&#xA;&#xA;void Proc1(Base&amp;amp; ref) {&#xA;  ref.fun();&#xA;}&#xA;&#xA;void Proc2(Base b) {&#xA;  b.fun();&#xA;}&#xA;&#xA;int main() {&#xA;  Derived d;&#xA;  Proc1(d);  // Derived: 3&#xA;  // 由于编译器看到的ref是Base&amp;amp;类型，所以这里的ref.fun()会被翻译成ref.fun(3)，但是运行期调用的是Derived中的fun函数，所以才会出现上面的结果&#xA;&#xA;  Proc2(d);  // Base: 3&#xA;  // 由于b不是d的引用或指针，而是由d构造出来的Base类型的数据，所以只会调用Base的成员函数&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;虚函数的调用成本高于非虚函数&#xA;&lt;ul&gt;&#xA;&lt;li&gt;C++缺省情况下不会把函数声明为虚函数（为了性能），Java所有的函数都是虚函数&lt;/li&gt;&#xA;&lt;li&gt;final关键字(见上面的代码)&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;要使用指针（或引用）引入动态绑定，比较上面代码中的Proc1和Proc2&lt;/li&gt;&#xA;&lt;li&gt;在构造函数中调用虚函数要小心，在基类中调用虚函数，这个函数不是派生类的实现&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  Base() {&#xA;    fun();&#xA;  }&#xA;&#xA;  virtual void fun() {&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;Base&amp;#34; &amp;lt;&amp;lt; std::endl;&#xA;  }&#xA;};&#xA;&#xA;struct Derived : public Base {&#xA;  void fun() override {&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;Derived&amp;#34; &amp;lt;&amp;lt; std::endl;&#xA;  }  &#xA;};&#xA;&#xA;int main() {&#xA;  Derived d;  // Base&#xA;  // 构造Derived的第一步是构造Base，而Base的构造函数中调用了fun，由于此时Base已经构造完成，Derived还没有构造完成，所以这里的fun是Base中的实现&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;派生类的析构函数会隐式调用基类的析构函数&lt;/li&gt;&#xA;&lt;li&gt;通常来说要将基类的析构函数声明为virtual的&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  ~Base() {&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;Base&amp;#34; &amp;lt;&amp;lt; std::endl;&#xA;  }&#xA;};&#xA;&#xA;struct Derived final : Base {&#xA;  ~Derived() {&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;Derived&amp;#34; &amp;lt;&amp;lt; std::endl;&#xA;  }  &#xA;};&#xA;&#xA;int main() {&#xA;  Derived* d = new Derived();&#xA;  Base* b = d;&#xA;  delete b;  // 只会打印&amp;#34;Base&amp;#34;&#xA;  // 因为Base的析构函数不是虚函数，所以delete b这行代码在编译期就会被确定为调用Base的析构函数&#xA;  // 但是这样就出问题了，因为Derived的析构函数没有被正常调用&#xA;  // 解决方案就是将Base的析构函数定义为虚函数: virtual ~Base() {}&#xA;&#xA;  // 但是并非任何情况下都需要将基类的析构函数定义为虚函数，比如下面这样不使用基类指针就行，只不过在大部分时候，我们之所以使用类的继承就是为了用基类的指针去挂载派生类的对象&#xA;  delete d;  // 会打印&amp;#34;Derived&amp;#34; &amp;#34;Base&amp;#34;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;在派生类中我们可以修改虚函数的访问权限&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;protected:&#xA;  virtual void fun() {}&#xA;};&#xA;&#xA;struct Derived : Base {&#xA;public:&#xA;  void fun() override {}  &#xA;};&#xA;&#xA;int main() {&#xA;  Derived d;&#xA;  d.fun();  // 可以通过编译，Derived中的fun被重写为public函数了&#xA;&#xA;  Base&amp;amp; b = d;&#xA;  b.fun();  // 编译器只知道b是Base&amp;amp;类型，但fun在Base中是protected函数，当然无法通过编译&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;继承与特殊成员&#xA;&lt;ul&gt;&#xA;&lt;li&gt;派生类的系统自动合成的……&#xA;&lt;ul&gt;&#xA;&lt;li&gt;缺省构造函数会隐式调用基类的缺省构造函数&lt;/li&gt;&#xA;&lt;li&gt;拷贝构造函数会隐式调用基类的拷贝构造函数&lt;/li&gt;&#xA;&lt;li&gt;赋值函数将隐式调用基类的赋值函数&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;派生类的析构函数会调用基类的析构函数&lt;/li&gt;&#xA;&lt;li&gt;派生类的其他构造函数将隐式调用基类的缺省构造函数&#xA;&lt;ul&gt;&#xA;&lt;li&gt;比如说，如果派生类的拷贝构造函数不是default(系统自动合成)的，而是有显示定义的，那在调用派生类的拷贝构造函数时，不会隐式调用基类的拷贝构造函数，而是调用的缺省构造函数&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;所有的特殊成员函数在显式定义时都可能需要显示调用基类相关成员&#xA;&lt;ul&gt;&#xA;&lt;li&gt;原因就是上一条描述的内容，调用方式类似于这样：Derived(const Derived&amp;amp; input) : Base(input) {}&lt;/li&gt;&#xA;&lt;li&gt;如果没有显示地调用&amp;quot;: Base(input)&amp;quot;，系统默认会调用Base的缺省构造函数，而非这里的拷贝构造函数&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;构造与销毁顺序&#xA;&lt;ul&gt;&#xA;&lt;li&gt;基类的构造函数会先调用，之后才涉及到派生类中数据成员的构造&lt;/li&gt;&#xA;&lt;li&gt;派生类中的数据成员会被先销毁，之后才涉及到基类的析构函数调用&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;补充知识&#xA;&lt;ul&gt;&#xA;&lt;li&gt;public，protected与private继承&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;public:&#xA;  // 基类、派生类、类外都可以访问&#xA;  int x;&#xA;private:&#xA;  // 基类可访问&#xA;  int y;&#xA;protected:&#xA;  // 基类、派生类可访问&#xA;  int z;&#xA;};&#xA;&#xA;// 无论Derived采用什么继承方式(public, private, protected)，上面的三种访问权限都是不变的。&#xA;// 区别在如下几个点：&#xA;// 1.public继承，Base中的public，protected和private属性在Derived中都保持不变，比如在Derived中x还是public，y还是private，z还是protected。&#xA;// 2.protected继承，Base中的public，protected在Derived中都变成protected，private属性则保持不变。&#xA;// 3.private继承，Base中的public，protected和private属性在Derived中都变成private。&#xA;struct Derived : public Base {};&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;&#xA;&lt;li&gt;public继承：描述“是一个”的关系&lt;/li&gt;&#xA;&lt;li&gt;private继承：描述“根据基类实现出“的关系，但是有更好的实现方式，就是把Base对象创建为Derived的一个私有成员变量，所以private继承很少用&lt;/li&gt;&#xA;&lt;li&gt;protected继承：几乎不会用&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;using与继承&#xA;&lt;ul&gt;&#xA;&lt;li&gt;使用using改变基类成员的访问权限&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;public:&#xA;  int x;&#xA;private:&#xA;  int y;&#xA;protected:&#xA;  int z;&#xA;  void fun() {}&#xA;};&#xA;&#xA;struct Derived : public Base {&#xA;public:&#xA;  using Base::z;&#xA;  using Base::fun;  // 所有的fun函数都会变成public，如果fun有重载的话，会作用到所有的fun上&#xA;private:&#xA;  using Base::x;&#xA;};&#xA;&#xA;int main() {&#xA;  Derived d;&#xA;  d.z;  // 有了上面“的using Base::z;”，这里就可以正常调用了&#xA;  d.x;  // 有了上面“的using Base::x;”，这里就会报错&#xA;  d.fun();  // 可以正常调用&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;&#xA;&lt;li&gt;注意以下两点！！&lt;/li&gt;&#xA;&lt;li&gt;要想通过using改变权限，首先该成员要对派生类可见，比如上面如果想把Base::y改成public或者protected就不行，因为对Derived来说，Base::y就不可见&lt;/li&gt;&#xA;&lt;li&gt;无法改变构造函数的访问权限&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;使用using继承基类的构造函数逻辑&#xA;&lt;ul&gt;&#xA;&lt;li&gt;Base中有多种自定义的构造函数(系统无法自动合成的)，且Derived和Base的数据成员和构造函数的实现逻辑又是一样的，如果再实现一遍就会很耗时，这时候就可以用using来把Base中的实现都复制过来&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;using与部分重写&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;protected:&#xA;  virtual void fun() {&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;1\n&amp;#34;;&#xA;  }&#xA;&#xA;  virtual void fun(int) {&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;2\n&amp;#34;;&#xA;  }&#xA;};&#xA;&#xA;struct Derived : public Base {&#xA;public:&#xA;  using Base::fun;&#xA;&#xA;  void fun(int) override {&#xA;    std::cout &amp;lt;&amp;lt; &amp;#34;3\n&amp;#34;;&#xA;  }&#xA;};&#xA;&#xA;int main() {&#xA;  Derived d;&#xA;  d.fun();  // 1&#xA;  d.fun(3);  // 3&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;继承与友元&#xA;&lt;ul&gt;&#xA;&lt;li&gt;友元关系无法继承，但基类的友元可以访问派生类中的基类的相关成员&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Derived;  // 声明，否则下面编译不通过&#xA;struct Base {&#xA;  friend void fun(const Derived&amp;amp;);&#xA;protected:&#xA;  int x = 10;&#xA;};&#xA;&#xA;struct Derived : public Base {&#xA;private:&#xA;  int y = 20;&#xA;};   &#xA;&#xA;void fun(const Derived&amp;amp; val) {&#xA;  std::cout &amp;lt;&amp;lt; val.x &amp;lt;&amp;lt; std::endl;  // 通过, 基类的友元可以访问派生类中的基类的相关成员&#xA;  std::cout &amp;lt;&amp;lt; val.y &amp;lt;&amp;lt; std::endl;  // 不通过，y不属于Base&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;派生类中的友元无法获得基类中成员的访问权限&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Derived;  // 声明，否则下面编译不通过&#xA;struct Base {&#xA;protected:&#xA;  int x = 10;&#xA;};&#xA;&#xA;struct Derived : public Base {&#xA;  friend void fun(const Derived&amp;amp;);&#xA;  friend void fun(const Base&amp;amp;);&#xA;private:&#xA;  int y = 20;&#xA;};   &#xA;&#xA;void fun(const Base&amp;amp; val) {&#xA;  std::cout &amp;lt;&amp;lt; val.x &amp;lt;&amp;lt; std::endl;  // 不通过, 派生类的友元不可以访问基类中的成员&#xA;}&#xA;&#xA;void fun(const Derived&amp;amp; val) {&#xA;  std::cout &amp;lt;&amp;lt; val.x &amp;lt;&amp;lt; std::endl;  // 不通过, 派生类的友元不可以访问基类中的成员&#xA;  std::cout &amp;lt;&amp;lt; val.y &amp;lt;&amp;lt; std::endl;  // 通过&#xA;}&#xA;// 如果上面的操作允许的话，所谓的protected和private的访问权限就形同虚设了，我只要搞个派生类，定义一个友元，就可以无限制地访问基类中所有成员，显然不合理&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;通过基类指针实现在容器中保存不同类型的对象&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  virtual double GetValue() = 0;&#xA;  virtual ~Base() = default;&#xA;};&#xA;&#xA;struct Derived : public Base {&#xA;  Derive(int x) : val(x) {}&#xA;  double GetValue() override { return val; }&#xA;private:&#xA;  int val;&#xA;};&#xA;&#xA;struct Derived2 : public Base {&#xA;  Derive2(double x) : val(x) {}&#xA;  double GetValue() override { return val; }&#xA;private:&#xA;  double val;&#xA;};&#xA;&#xA;int main() {&#xA;  std::vector&amp;lt;std::shared_ptr&amp;lt;Base&amp;gt;&amp;gt; vec;&#xA;  vec.emplace_back(new Derived(1));&#xA;  vec.emplace_back(new Derived2(3.14));&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;多重继承&#xA;&lt;ul&gt;&#xA;&lt;li&gt;Derived继承自Base1和Base2，我们就可以使用Base1或者Base2的指针来保存Derived对象&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;虚继承&#xA;&lt;ul&gt;&#xA;&lt;li&gt;错误示例&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  virtual ~Base() = default;&#xA;  int x;&#xA;};&#xA;&#xA;struct Base1 : Base {&#xA;  virtual ~Base1() = default;&#xA;};&#xA;&#xA;struct Base2 : Base {&#xA;  virtual ~Base2() = default;&#xA;};&#xA;&#xA;struct Derived : public Base1, public Base2 {};&#xA;&#xA;int main() {&#xA;  Derived d;&#xA;  std::cout &amp;lt;&amp;lt; &amp;amp;(d.Base1::x) &amp;lt;&amp;lt; &amp;#34;\n&amp;#34;;&#xA;  std::cout &amp;lt;&amp;lt; &amp;amp;(d.Base2::x) &amp;lt;&amp;lt; &amp;#34;\n&amp;#34;;  // 通过，且地址一样的&#xA;  d.x;  // 报错，不知道是Base1中的x还是Base2中的x&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;正确示例&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  virtual ~Base() = default;&#xA;  int x;&#xA;};&#xA;&#xA;struct Base1 : virtual Base {&#xA;  virtual ~Base1() = default;&#xA;};&#xA;&#xA;struct Base2 : virtual Base {&#xA;  virtual ~Base2() = default;&#xA;};&#xA;&#xA;struct Derived : public Base1, public Base2 {};&#xA;&#xA;int main() {&#xA;  Derived d;&#xA;  d.x;  // 正确，使用了虚继承&#xA;  std::cout &amp;lt;&amp;lt; &amp;amp;(d.Base1::x) &amp;lt;&amp;lt; &amp;#34;\n&amp;#34;;&#xA;  std::cout &amp;lt;&amp;lt; &amp;amp;(d.Base2::x) &amp;lt;&amp;lt; &amp;#34;\n&amp;#34;;&#xA;  std::cout &amp;lt;&amp;lt; &amp;amp;(d.x) &amp;lt;&amp;lt; &amp;#34;\n&amp;#34;;  // 正确，使用了虚继承&#xA;  // 上面三个地址的输出都是一样的&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;空基类优化与[[no unique address]]属性&#xA;&lt;ul&gt;&#xA;&lt;li&gt;未优化代码&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  void fun() {}  // 成员函数不会占用类的大小&#xA;};&#xA;// 类所占内存的大小是由成员变量（静态变量除外）决定的，虚函数指针和虚基类指针也属于数据部分，成员函数是不计算在内的。&#xA;// 在编译器处理后，成员变量和成员函数是分离的。成员函数还是以一般的函数一样的存在。&#xA;// a.fun()是通过fun(a.this)来调用的。所谓成员函数只是在名义上是类里的。&#xA;&#xA;struct Derived {&#xA;  int x;&#xA;  Base b;  // 为了调用Base中的一些函数，把Base对象声明为了成员变量&#xA;};&#xA;&#xA;int main() {&#xA;  std::cout &amp;lt;&amp;lt; sizeof(Base) &amp;lt;&amp;lt; std::endl;  // 1，没有fun函数这里还是1&#xA;  std::cout &amp;lt;&amp;lt; sizeof(Derived) &amp;lt;&amp;lt; std::endl;  // 8，本来应该是5，编译器会做padding&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;优化后代码&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  void fun() {}&#xA;};&#xA;&#xA;struct Derived : Base {&#xA;  int x;&#xA;};&#xA;&#xA;int main() {&#xA;  std::cout &amp;lt;&amp;lt; sizeof(Base) &amp;lt;&amp;lt; std::endl;  // 1&#xA;  std::cout &amp;lt;&amp;lt; sizeof(Derived) &amp;lt;&amp;lt; std::endl;  // 4，空基类优化，如果基类中不包含任何数据成员，占用的内存会被省去&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;上面的优化代码仍然不够好，因为Derived并不是真的想继承Base（Derived不是一个Base），只是想用Base中的方法。虽然代码功能实现了，但是表达的内容不是那么精确，所以C++20引入了[[no_unique_address]]&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Base {&#xA;  void fun() {}  // 成员函数不会占用类的大小&#xA;};&#xA;&#xA;struct Derived {&#xA;  int x;&#xA;  [[no_unique_address]] Base b;  // 为了调用Base中的一些函数，把Base对象声明为了成员变量&#xA;};&#xA;&#xA;int main() {&#xA;  std::cout &amp;lt;&amp;lt; sizeof(Base) &amp;lt;&amp;lt; std::endl;  // 1，没有fun函数这里还是1&#xA;  std::cout &amp;lt;&amp;lt; sizeof(Derived) &amp;lt;&amp;lt; std::endl;  // 4&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
        </item><item>
            <title>Local Class</title>
            <link>https://example.org/post/coding/c&#43;&#43;/class/local-class/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/class/local-class/</guid>
            <description>&lt;h1 id=&#34;局部类&#34;&gt;局部类&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;可以在函数中定义&lt;/li&gt;&#xA;&lt;li&gt;可以访问外围函数中定义的类型声明，静态对象与枚举&lt;/li&gt;&#xA;&lt;li&gt;可以定义成员函数，但成员函数的定义必须放在类内&lt;/li&gt;&#xA;&lt;li&gt;不能定义静态数据成员&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;void fun() {&#xA;  using MyInt = int;&#xA;  int val;&#xA;  struct Helper {&#xA;    MyInt x;&#xA;    int y = val;  // 编译失败&#xA;    inline static int val2 = 100;  // 编译失败 &#xA;&#xA;    int inc();&#xA;    int acc() {  // 编译成功，函数定义在类内部&#xA;      x++;&#xA;    }&#xA;  };&#xA;&#xA;  int Helper::inc() {  // 编译失败，函数内部不能再定义函数&#xA;    returen x++;&#xA;  }&#xA;  Helper h;&#xA;}&#xA;&#xA;int main() {&#xA;  fun();&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
        </item><item>
            <title>Member Ptr of Class</title>
            <link>https://example.org/post/coding/c&#43;&#43;/class/member-ptr/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/class/member-ptr/</guid>
            <description>&lt;h1 id=&#34;成员指针&#34;&gt;成员指针&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;数据成员指针类型示例：int A::*;&lt;/li&gt;&#xA;&lt;li&gt;成员函数指针类型示例：int (A::*)(double);&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;class Str {&#xA;public:&#xA;  int x;&#xA;&#xA;  void fun() {};&#xA;  void fun(double) {};&#xA;};&#xA;int main() {&#xA;  int Str::*ptr = &amp;amp;Str::x;  // 数据成员指针&#xA;  // 阅读方法：*表明ptr是个指针，能够访问Str::域内的内容，并且这个成员的类型是int&#xA;&#xA;  int Str::*ptr = &amp;amp;(Str::x);  // 报错，加了括号后含义就变了，就不再是取某个域内的地址了，而是取括号里的内容的地址，这就要求括号内的东西是有定义的，但Str::x目前还没有定义，只是声明，所以会报错&#xA;&#xA;  void (Str::* ptr_fun)() = &amp;amp;Str::fun;  // 成员函数指针&#xA;  // 阅读方法：*表明ptr是个指针，能够访问Str::域内的内容，()表示这个东西是个函数，函数的输入是void(省略了)，输出也是void&#xA;&#xA;  auto ptr_fun = &amp;amp;Str::fun;  // 报错，因为无法确定auto的内容，上面有两个fun函数可以选择&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;成员指针对象赋值：auto ptr = &amp;amp;A::x;&#xA;&lt;ul&gt;&#xA;&lt;li&gt;注意不能加()，上面有原因&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;成员指针的使用&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;class Str {&#xA;public:&#xA;  int x;&#xA;};&#xA;&#xA;int main() {&#xA;  int Str::*ptr = &amp;amp;Str::x;&#xA;  *ptr = 1;  // 报错，ptr指向的只是个声明，并没有明确的定义，所以就没有一块固定的内存来解引用&#xA;&#xA;  Str obj;&#xA;  obj.*prt = 1;  // 合理&#xA;  Str* p_obj = &amp;amp;obj;&#xA;  p_obj-&amp;gt;*ptr = 3;  // 合理&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;bind交互&#xA;&lt;ul&gt;&#xA;&lt;li&gt;使用bind + 成员指针构造可调用对象&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;class Str {&#xA;public:&#xA;  int x;&#xA;&#xA;  void fun(double) {};&#xA;};&#xA;&#xA;int main() {&#xA;  auto ptr = &amp;amp;Str::fun();&#xA;&#xA;  Str obj;&#xA;  (obj.*ptr)(100.0);&#xA;&#xA;  auto x = std::bind(ptr, obj, 100.0);  // 没有obj的话会报错 &#xA;  x();  // 调用fun&#xA;&#xA;  auto ptr2 = &amp;amp;Str::x;&#xA;  auto x2 = std::bind(ptr2, obj);&#xA;  x2();  // 返回obj.x&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;注意这种方法也可以基于数据成员指针构造可调用对象(见上面代码中的ptr2)&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
        </item><item>
            <title>Nested Class</title>
            <link>https://example.org/post/coding/c&#43;&#43;/class/nested-class/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/class/nested-class/</guid>
            <description>&lt;h1 id=&#34;嵌套类&#34;&gt;嵌套类&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;在类中定义的类&#xA;&lt;ul&gt;&#xA;&lt;li&gt;嵌套类有自己的域，与外围类的域形成嵌套关系&#xA;&lt;ul&gt;&#xA;&lt;li&gt;嵌套类中的名称查找失败时会在其外围类中继续查找&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;class Out {&#xA;  using MyInt = int;&#xA;  inline static int val2 = 3;&#xA;  int xxx = 100;&#xA;public:&#xA;  class In {&#xA;  public:&#xA;    inline static MyInt val = val2;&#xA;    int vvv = xxx;  // 会报错，上面的int xxx不是定义，而是声明，只有在类被实例化的时候才会创建，所以这里无法对齐进行赋值； 如果有static关键字，就是定义了，所以上面一行合法&#xA;  };&#xA;};&#xA;&#xA;int main() {&#xA;  Out::In obj;&#xA;  Out::In::val;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;嵌套类和外围类单独拥有各自的成员&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
        </item><item>
            <title>Operator</title>
            <link>https://example.org/post/coding/c&#43;&#43;/class/operator/</link>
            <pubDate>Wed, 02 Apr 2025 10:32:39 +0800</pubDate>
            <guid>https://example.org/post/coding/c&#43;&#43;/class/operator/</guid>
            <description>&lt;h1 id=&#34;运算符重载&#34;&gt;运算符重载&#xA;&lt;/h1&gt;&lt;ul&gt;&#xA;&lt;li&gt;使用operator关键字引入重载函数&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  int val = 3;&#xA;&#xA;  auto operator () (int y = 3) {  // 只有operator () 可以有缺省参数&#xA;    return val + x;&#xA;  }&#xA;&#xA;  auto operator + (const Str&amp;amp; x) {&#xA;    Str res;&#xA;    res.val = val + x.val;&#xA;    return res;&#xA;  }&#xA;};&#xA;&#xA;Str Add(const Str&amp;amp; x, const Str&amp;amp; y) {&#xA;  Str z;&#xA;  z.val = x.val + y.val;&#xA;  return z;&#xA;}&#xA;&#xA;// auto自动推断返回值需要在C++11后面的版本才可以编译通过&#xA;auto operator + (const Str&amp;amp; x, const Str&amp;amp; y) {&#xA;  Str res;&#xA;  res.val = x.val + y.val;&#xA;  return res;&#xA;}&#xA;&#xA;int main() {&#xA;  Str x;&#xA;  Str y;&#xA;&#xA;  auto z = x + y;&#xA;  z = Add(x, y);&#xA;&#xA;  z = x(4);  // z.val = 3 + 4&#xA;  z = x();  // z.val = 3 + 3&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;&#xA;&lt;li&gt;重载不能发明新的运算(比如不能创造一个@运算符)，不能改变运算的优先级与结合性，通常不改变运算含义。&lt;/li&gt;&#xA;&lt;li&gt;函数参数个数与运算操作数个数相同，至少一个为类类型&lt;/li&gt;&#xA;&lt;li&gt;除operator()外其他运算符不能有缺省参数&lt;/li&gt;&#xA;&lt;li&gt;可以选择实现为成员函数与非成员函数&#xA;&lt;ul&gt;&#xA;&lt;li&gt;通常来说，实现为成员函数会以*this作为第一个操作数(注意==与&amp;lt;==&amp;gt;的重载可以不需要*this作为第一个操作数)&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;根据重载特性，可以将运算符进一步划分&#xA;&lt;ul&gt;&#xA;&lt;li&gt;可重载且必须实现为成员函数的运算符(=,[],(),-&amp;gt;与转型运算符)&lt;/li&gt;&#xA;&lt;li&gt;可重载且可以实现为非成员函数的运算符&lt;/li&gt;&#xA;&lt;li&gt;可重载但不建议重载的运算符(&amp;amp;&amp;amp;,||,逗号运算符)&#xA;&lt;ul&gt;&#xA;&lt;li&gt;C++17中规定了相应的求值顺序但没有方式实现短路逻辑(短路逻辑即:A&amp;amp;&amp;amp;B,如果A为非，B不用执行)&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;不可重载的运算符(如 ?: 三元运算符)&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;相对来说比较特殊的运算符重载&#xA;&lt;ul&gt;&#xA;&lt;li&gt;对称运算符通常定义为非成员函数以支持首个操作数的类型转换&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;// 错误示范&#xA;struct Str {&#xA;  Str(int x) : val(x) {}&#xA;  auto operator + (const Str&amp;amp; input) {&#xA;    return Str(val + input.val);&#xA;  }&#xA;  int val;&#xA;};&#xA;&#xA;int main() {&#xA;  Str x(3);&#xA;  Str y = x + 4;  // 通过，4会隐式转换为Str(4)&#xA;  Str z = 4 + x;  // 不通过，最好不要将称运算符定义为成员函数&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;// 正确示范&#xA;struct Str {&#xA;  Str(int x) : val(x) {}&#xA;&#xA;  Str&amp;amp; operator= (const std::string&amp;amp; input) {&#xA;    val = static_cast&amp;lt;int&amp;gt;(input.size());&#xA;    return *this;&#xA;  }&#xA;&#xA;  // 定义为友元的原因是val是私有成员，而operator +是一个类外的运算符重载函数&#xA;  friend auto operator + (const Str&amp;amp; input1, const Str&amp;amp; input2) {&#xA;    return Str(input1.val + input2.val);&#xA;  }&#xA;&#xA;  // 注意这里要返回引用，因为输出流是不支持拷贝的&#xA;  friend auto&amp;amp; operator &amp;lt;&amp;lt; (std::ostream&amp;amp; ostr, const Str&amp;amp; input) {&#xA;    ostr &amp;lt;&amp;lt; input.val;&#xA;    return ostr;&#xA;  }&#xA;&#xA;  int&amp;amp; operator[] (int id) {  // func1&#xA;    return val;&#xA;  }&#xA;&#xA;  int operator[] (int id) const {  // func2&#xA;    return val;&#xA;  }&#xA;&#xA;private:&#xA;  int val;&#xA;};&#xA;&#xA;int main() {&#xA;  Str x = 3;&#xA;  x = &amp;#34;1234&amp;#34;;&#xA;  Str y = 4 + x;  // 合法，我们在类外定了operator + (const Str&amp;amp;, const Str&amp;amp;)函数，4会被隐式转换为Str(4)&#xA;  std::cout &amp;lt;&amp;lt; x &amp;lt;&amp;lt; y;  // 合法&#xA;&#xA;  std::cout &amp;lt;&amp;lt; x[0];  // 合法&#xA;  x[0] = 1;  // 如果func1不是返回的引用，就不合法，因为右值一般不能出现在等号左边&#xA;&#xA;  const Str cx = 3;&#xA;  std::cout &amp;lt;&amp;lt; cx[0];  // 如果没有定义func2，就不合法，因为func1没用const修饰，可能对类本身进行修改&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;移位运算符一定要定义为非成员函数，因为其首个操作数类型需要是流类型(如上面代码所示)&lt;/li&gt;&#xA;&lt;li&gt;赋值运算符也可以接受一般参数，比如上面代码中传入了一个string。&lt;/li&gt;&#xA;&lt;li&gt;operator []通常返回引用，某些情况下不返回引用，比如上面的func2&lt;/li&gt;&#xA;&lt;li&gt;自增、自减运算符的前缀、后缀重载法&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;Str&amp;amp; operator++ () {  //如果()里是空的，对应前缀自增x++&#xA;  ++val;&#xA;  return *this;&#xA;}&#xA;Str operator++ (int) {  // 如果()里有个int变量，对应后缀自增++x，注意这里的()里面的变量没有任何意义，不会参与任何计算或者赋值&#xA;  Str tmp(*this);&#xA;  ++val;&#xA;  return tmp;&#xA;}&#xA;// 从上面的代码可以看出，能用前缀自增就用前缀，更加高效&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;使用解引用运算符(*)与成员访问运算符(-&amp;gt;)模拟指针行为&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;public:&#xA;  Str(int* p) : ptr(p) {}&#xA;&#xA;  int&amp;amp; operator * () {&#xA;    return *ptr;&#xA;  }&#xA;&#xA;  Str* operator -&amp;gt; () {&#xA;    return this;&#xA;  }&#xA;&#xA;  int val = 5;&#xA;&#xA;private:&#xA;  int* ptr;&#xA;};&#xA;&#xA;int main() {&#xA;  int x = 100;&#xA;  Str ptr(&amp;amp;x);&#xA;  std::cout &amp;lt;&amp;lt; *ptr &amp;lt;&amp;lt; std::endl;&#xA;  std::cout &amp;lt;&amp;lt; ptr-&amp;gt;val &amp;lt;&amp;lt; std::endl;&#xA;  // C++编译的时候遇到-&amp;gt;就会去查看有没有operator-&amp;gt;重载，如果有重载的话，就把它当作一个类成员函数去调用&#xA;  // 所以ptr-&amp;gt;val会被翻译为：ptr.operator-&amp;gt;()-&amp;gt;val，其中ptr.operator-&amp;gt;()的返回值就是一个Str*类型的指针&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;ul&gt;&#xA;&lt;li&gt;注意，&amp;quot;.&amp;ldquo;运算符不能重载&lt;/li&gt;&#xA;&lt;li&gt;&amp;ldquo;-&amp;gt;&amp;ldquo;会递归调用&amp;rdquo;-&amp;gt;&amp;ldquo;操作&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str2 {&#xA;  Str2* operator -&amp;gt; () {&#xA;    return this;&#xA;  }&#xA;&#xA;  int val2 = 123;&#xA;};&#xA;&#xA;struct Str {&#xA;  Str2 operator -&amp;gt; () {&#xA;    return Str2{};&#xA;  }&#xA;&#xA;  int val = 5;&#xA;};&#xA;&#xA;int main() {&#xA;  Str ptr = Str();&#xA;  std::cout &amp;lt;&amp;lt; ptr-&amp;gt;val2 &amp;lt;&amp;lt; std::endl;&#xA;  // 由于Str的operator-&amp;gt;()函数返回的是Str2，不是一个指针类型，所以编译期还会继续调用Str2的operator-&amp;gt;()函数，直到返回一个指针为止&#xA;  // ptr.operator-&amp;gt;().operator()-&amp;gt;val2&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;使用函数调用运算符构造可调用对象(lambda表达式就是通过这个手段实现的)&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  Str(int p) : val(p) {}&#xA;&#xA;  bool operator () (int input) {&#xA;    return val++ &amp;lt; input;&#xA;  }&#xA;&#xA;private:&#xA;  int val;&#xA;};&#xA;&#xA;int main() {&#xA;  Str obj(100);&#xA;  std::cout &amp;lt;&amp;lt; obj(99) &amp;lt;&amp;lt; std::endl;  // 0&#xA;  std::cout &amp;lt;&amp;lt; obj(102) &amp;lt;&amp;lt; std::endl;  // 1&#xA;  std::cout &amp;lt;&amp;lt; obj(102) &amp;lt;&amp;lt; std::endl;  // 0, 因为val++&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;类型转换运算符&#xA;&lt;ul&gt;&#xA;&lt;li&gt;函数声明为operator type() const&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  Str(int p) : val(p) {}&#xA;&#xA;  operator int() const {&#xA;    return val;&#xA;  }&#xA;&#xA;  friend auto operator + (Str a, Str b) {&#xA;    return Str(a.val + b.val);&#xA;  }&#xA;&#xA;private:&#xA;  int val;&#xA;};&#xA;&#xA;int main() {&#xA;  Str obj(100);&#xA;  static_cast&amp;lt;Str&amp;gt;(100);  // 通过&#xA;  static_cast&amp;lt;int&amp;gt;(obj);  // 通过&#xA;  int v = obj;  // 通过&#xA;  std::cout &amp;lt;&amp;lt; v &amp;lt;&amp;lt; std::endl;&#xA;&#xA;  obj + 3;  // 不通过，编译器既可以选择把obj转为int再相加，也可以把3转换为Str类型再相加（上面实现了Str的+友元重载）&#xA;  // 解决方案就是在Str的构造函数或者型转换运算符重载函数前面加上explicit关键字&#xA;  // 如果在构造函数前加explicit，3就不能隐式转换为Str类型了&#xA;  // 注意，不能两个函数前同时加上explicit，如果都加了也编译不过，因为没有任何隐式转换发生的话，Str和int无法执行+。&#xA;&#xA;  // 如果在两个函数前同时加上explicit，代码就要这么改&#xA;  obj + static_cast&amp;lt;Str&amp;gt;(3);&#xA;  static_cast&amp;lt;int&amp;gt;(obj) + 3;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;与单参数构造函数一样，都引入了一种类型转换方式&lt;/li&gt;&#xA;&lt;li&gt;注意避免引入歧义性与意料之外的行为&#xA;&lt;ul&gt;&#xA;&lt;li&gt;通过explicit引入显示类型转换(参考上面的代码)&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;explicit bool的特殊性：用于条件表达式时会进行隐式类型转换&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;struct Str {&#xA;  explicit Str(int p) : val(p) {}&#xA;&#xA;  explicit operator bool() const {&#xA;    return val == 0;&#xA;  }&#xA;&#xA;private:&#xA;  int val;&#xA;};&#xA;&#xA;int main() {&#xA;  Str obj(100);    &#xA;  std::cout &amp;lt;&amp;lt; obj &amp;lt;&amp;lt; std::endl;  // 不通过，因为上面有explicit修饰，不能隐式转换了&#xA;&#xA;  if (obj) {  // 通过，即使定义了explicit，但这里仍然会进行隐式类型转换&#xA;    std::cout &amp;lt;&amp;lt; 1 &amp;lt;&amp;lt; std::endl;&#xA;  } else {&#xA;    std::cout &amp;lt;&amp;lt; 0 &amp;lt;&amp;lt; std::endl;&#xA;  }&#xA;&#xA;  auto var = obj ? 1 : 0;  // 通过&#xA;  std::cout &amp;lt;&amp;lt; var &amp;lt;&amp;lt; std::endl;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;li&gt;C++20中对==与&amp;lt;==&amp;gt;的重载&#xA;&lt;ul&gt;&#xA;&lt;li&gt;在C++20之前，如果我们想重载大小比较，我们需要实现6个重载，==, !=, &amp;gt;=, &amp;lt;=, &amp;gt;, &amp;lt;。&lt;/li&gt;&#xA;&lt;li&gt;在C++20之后，只需要重载==, &amp;lt;==&amp;gt;这两个就可以完成对上面6个功能的定义&lt;/li&gt;&#xA;&lt;li&gt;通过==定义!=&lt;/li&gt;&#xA;&lt;li&gt;隐式交换操作数&#xA;&lt;pre tabindex=&#34;0&#34;&gt;&lt;code&gt;#include &amp;lt;compare&amp;gt;&#xA;struct Str {&#xA;  Str(int p) : val(p) {}&#xA;&#xA;  friend bool operator == (Str obj, int obj2) {&#xA;    return obj.val == obj2;&#xA;  }&#xA;&#xA;  auto operator &amp;lt;==&amp;gt; (int x) {&#xA;    return val &amp;lt;==&amp;gt; x;&#xA;  }&#xA;&#xA;private:&#xA;  int val;&#xA;};&#xA;&#xA;int main() {&#xA;  Str obj(100);&#xA;  std::cout &amp;lt;&amp;lt; (obj == 100) &amp;lt;&amp;lt; std::endl;&#xA;  std::cout &amp;lt;&amp;lt; (obj != 100) &amp;lt;&amp;lt; std::endl;  // C++20之后通过&#xA;  std::cout &amp;lt;&amp;lt; (100 == obj) &amp;lt;&amp;lt; std::endl;  // C++20之后通过，C++20会先尝试找(int, Str)，如果找不到会继续找(Str, int)&#xA;&#xA;  std::cout &amp;lt;&amp;lt; (100 &amp;gt;= obj) &amp;lt;&amp;lt; std::endl;&#xA;  std::cout &amp;lt;&amp;lt; (obj &amp;gt;= 100) &amp;lt;&amp;lt; std::endl;&#xA;}&#xA;&lt;/code&gt;&lt;/pre&gt;&lt;/li&gt;&#xA;&lt;li&gt;通过&amp;lt;==&amp;gt;定义多种比较逻辑(参考上main的代码)&#xA;&lt;ul&gt;&#xA;&lt;li&gt;注意&amp;lt;==&amp;gt;可以返回的类型，strong_ordering, weak_ordering, partial_ordering&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;&lt;/li&gt;&#xA;&lt;/ul&gt;&#xA;</description>
        </item></channel>
</rss>
